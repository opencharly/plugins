# skills: the GPU driver-mode switch reclaims charly's own stray GPU pods before a nvidia→vfio flip

Aligns the two skills that document the vfio↔nvidia switch with the landed arbiter behavior
(superproject `fix(arbiter): reclaim leaked/stray GPU pods before a nvidia->vfio flip`): before any
nvidia→vfio flip the arbiter first reclaims every running charly GPU-CDI pod that no active lease
covers (a leaked/stray disposable bed whose transient owner-PID-tied lease died with its
check-runner), gracefully stopping each so its `nvidia_uvm` handle is freed — so a leaked GPU bed can
never wedge the flip, and a residual `EBUSY` is then a GENUINE EXTERNAL (non-charly) client.

- `internals/skills/disposable/SKILL.md` "The mode flip" — adds the reclaim step (the detail owner).
- `vm/skills/vm/SKILL.md` "Why the switch is safe by construction" — reframes the EBUSY refusal as
  "a genuine external client remains after charly's own pods are reclaimed", pointing to the disposable
  skill for detail.

Present-tense skill alignment only; no behavior change (the behavior landed in the superproject).

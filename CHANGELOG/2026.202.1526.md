# 2026.202.1526 — FLOOR-SLIM Unit 4: R5 sweep of the CheckResult exception claim

Finalized by the fresh `pr-validator` at merge.

## What this is

Documentation-only (R1/R5 stale-claim sweep): FLOOR-SLIM Unit 4's CheckResult
wire-envelope split (the `sdk`/superproject legs of this same cutover — see
their CHANGELOG entries) narrowed the `kit.CheckResult` hand-written-exception
claim: `spec.CheckResult` (CUE-sourced) is now the base, with
`kit.CheckResult` an EMBEDDING WRAPPER adding back only the one genuinely
inexpressible field (`DeadlineExceeded bool json:"-"`) — not a hand-written
type in its entirety, as `/charly-internals:go`'s SDD section previously
(and, until this fix, still) stated.

`plugins/internals/skills/go/SKILL.md`'s "json:\"-\" fields" bullet is
updated to describe the narrower, current shape and to generalize the
lesson: a single-field `json:"-"` exception should be an embed-wrapper
around a CUE-sourced base, not grounds to keep an entire type hand-written.

## Verification

Documentation-only change, zero behavior change. Non-runtime standards:
adversarial consistency review against the actual `sdk/kit/checkresult.go` +
`sdk/schema/checkresult.cue` shape (this cutover's own code, cross-checked);
markdown integrity; no other stale references found (`git grep
DeadlineExceeded` + `'hand-written result model'` across sdk/charly/candy
swept clean).

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; documentation reviewed)

# 2026.193.1125 — D2: commit the memory-only insights (gengotypes spike table, delegate-claim verification, the memory-vs-skills leak rule)

Documentation-only cutover (D2). Operator directive: *"saving things only to memory is going
to make every charly user have the same issue you've just solved"* — memory is per-operator
(Syncthing-private) and invisible to every other user AND every fresh validator/teammate, so
generalizable proven insights that lived only in memory are committed to their owning skills.

**`internals/skills/go/SKILL.md`** (SDD "Generation coverage"):
- The spike-proven exception set grows three→FOUR: **`json:"-"` (keep-in-Go, drop-from-wire)
  fields are genuinely inexpressible** — gengotypes emits a real `json:"<name>,omitempty"` tag
  instead (RDD spike 2026-07-12, the P12 check seam: `kit.CheckResult.DeadlineExceeded`); a
  hand-written type carrying one is a LEGAL documented exception with the spike cited.
- New **spike-proven CAN/CANNOT quick-reference table** consolidating every live gengotypes
  result (discriminated structs, typed def references, `@go(,type=…)` scalar overrides,
  required-`!`-PascalCase tag fidelity, recursive pointer slices, string-keyed maps — vs
  disjunctions, open tails, int-keyed maps, `json:"-"`): reuse settled results, re-spike only
  NEW shape classes.

**`internals/skills/agents/SKILL.md`** ("Delegation IS fresh context"):
- New rule: **verify every delegate decision — a teammate/sub-agent report is a CLAIM**
  (never-trust-verify applies to delegates exactly as to docs/memory). The orchestrator
  verifies each decision's 1–3 load-bearing claims itself (read/grep/spike by risk), records
  the verification in the ruling, flags the unverifiable rather than trusting silently;
  validator findings and delegates' claims about EACH OTHER'S scope get the same treatment.
  Live examples from one orchestration wave recorded (the "additive" move that was a
  cross-cutover type hub; the wire-exception rationale a mandated spike overturned; twin wire
  types defined in two schema files within minutes).

**`internals/skills/skills/SKILL.md`** ("Two-Layer Sync Architecture"):
- New rule: **a generalizable proven insight saved only to memory is a knowledge LEAK** —
  codify it into the owning committed skill IMMEDIATELY as a docs-only cutover on proof, never
  batched to a program-end docs phase. The placement test: *would a stranger cloning the repo
  want this?* Yes → committed skill now; only-you → memory.

# 2026.197.0612 — R5 claim-keyed sweep now explicitly binds to the cutover's OWN new prose

Documentation-only cutover, sourced from a proven R5 gap: the strict-policy skill's
claim-keyed-sweep language ("fix EVERY doc/skill/comment repeating the same
false/outdated/misleading claim") read, in practice, as covering only PRE-EXISTING
surfaces — every worked example and every prior sweep swept sibling *skills*, never
the cutover's own freshly-authored narrative.

## The motivating incident

P11c (opencharly/charly#126, merged as `6a99be6`, tagged `v2026.196.1739`) round-1
FAILED validation for exactly this gap: the R5 claim-keyed sweep correctly rewrote
the stale `PodDeployTarget`/in-core-`OCITarget` claims across every candy
description and skill, but the SAME cutover's own CHANGELOG entry introduced a new
false claim of its own — it described the dissolved render as "compiled-in" in a
way that no longer matched the post-dissolution architecture. The sweep had been
keyed to pre-existing surfaces only; nothing keyed it to the narrative being
written that very cutover to describe the fix.

## The fix

Added a concise clarifying sentence, in the SAME register as the surrounding prose,
to each of the three surfaces that state the claim-keyed sweep:

- `internals/skills/strict-policy/SKILL.md`, R1 ("Documentation divergence is a
  failure for R1 purposes") — the sweep now explicitly binds to a CHANGELOG entry,
  PR description, or commit message drafted in the SAME cutover.
- `internals/skills/strict-policy/SKILL.md`, R5 ("Identifier-keyed vs claim-keyed
  sweep") — same clarification, phrased for the hard-cutover framing.
- `internals/skills/skills/SKILL.md`, "Skill↔code source-map sync audit" step 4
  ("Claim-keyed sweep (R5)") — same clarification, scoped to the CHANGELOG/PR/commit
  triad this audit procedure itself produces.

No identifier was deleted or renamed; this is a claim-level clarification only (R1's
"documentation divergence is a failure" applies to the claim's own incompleteness,
not to a stale reference).

## Change class + tier

Documentation-only — 2 skill files + this CHANGELOG entry, zero behavior change, no
Go / YAML-schema / hook / workflow code. Gate: the non-runtime standards (adversarial
consistency review, R5 grep self-test, cross-reference validation, markdown
integrity, the `pre-commit-gate.sh` / `pre-push-gate.sh` gates). Tier:
`documentation reviewed`. The superproject leg bumps the `plugins` gitlink to this
merged commit in its own follow-on PR.

*Assisted-by: Claude (documentation reviewed)*

# 2026.194.1121 — P15 R5 sweep: skill refs to the box verbs that moved to command:box

2026-07-13

Documentation-only — the plugins half of the P15 cutover's R5 stale-reference sweep. P15 moved
the build-mode `charly box` verb HANDLERS (`generate`/`validate`/`new`/`pkg`) out of charly core
into the new COMPILED-IN command plugin `candy/plugin-box` (nested under the `box` grammar group).
Two skill paragraphs named identifiers that P15 deleted; both are corrected here. The superproject
half (the code move + `candy/plugin-box/` + the CHANGELOG narrative) lands in the paired
superproject PR; this plugins PR lands first, then the superproject bumps its `plugins` pointer to
this merge.

**`build/skills/generate/SKILL.md`** — the "Build-engine dispatch" paragraph named `GenerateCmd.Run`
(a deleted core Kong struct) as the generate dispatcher. Rewritten: `charly box generate` is now
served by the compiled-in `command:box` plugin (candy/plugin-box), whose generate handler invokes
the peer `build:generate` plugin over InvokeProvider. The `build:generate` → `HostBuild("build-resolve",
GenerateOnly)` seam itself is unchanged.

**`internals/skills/go/SKILL.md`** — the `scaffold_cmds.go` file-map row listed `NewProjectCmd` and
`NewBoxCmd` among its structs; both moved to `candy/plugin-box` with the `box new project/box/candy`
scaffold group in P15. The row now lists only the retained authoring verbs (`BoxSetCmd`,
`BoxAddCandyCmd`, `BoxRmCandyCmd`, `BoxFetchCmd`, `BoxRefreshCmd`, `BoxWriteCmd`, `BoxCatCmd`) and
notes the `new` group's relocation.

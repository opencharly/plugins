# 2026.206.0002 — plugins — R5 sweep: retention engine relocated to candy/plugin-clean

Placeholder CalVer — the fresh `pr-validator` finalizes the merge-time version.

## What changed

The K1-alpha core-minimization wave relocated charly's shared build-artifact
retention engine (image-tag / build-candy / check-run pruning + the `--deep`
store-wide dangling-image purge + the charly-labeled image-tag inventory) out
of `charly/retention.go` and into the compiled-in `candy/plugin-clean` plugin
(a new `verb:retention` capability, mirroring `verb:credential`/`verb:gpu`/
`verb:tunnel`), replacing the old `charly/host_build_retention.go` seam with a
much smaller `retention-defaults` `HostBuild` seam for the one thing the
plugin genuinely cannot compute itself.

Two skills described the pre-relocation architecture as permanent
("`charly/retention.go` STAYS in core because it is multi-caller") — this is
a documentation-divergence R1 incident once the code moved, so this cutover
sweeps both:

- `core/skills/clean/SKILL.md` — the `charly clean` reference: rewrites the
  "Overview" paragraph and the "Implementation" section to describe the
  ENGINE now living in `candy/plugin-clean/retention.go`, the `verb:retention`
  capability, the three real callers (core's `charly box build` post-build
  prune + `charly box list tags`, and `candy/plugin-check`'s post-run prune
  hook reaching it over the `InvokeProvider` peer-dispatch leg), and the
  narrowed `retention-defaults` `HostBuild` seam.
- `internals/skills/go/SKILL.md` — the `sdk/kit/local_image.go` file-map row
  repoints its `charly/retention.go calls them from package main now` claim
  at `candy/plugin-clean/retention.go`.

Documentation-only change (two `*.md` skill files, zero behavior change) —
the Documentation-only change class.

## Verification

- Adversarial consistency review: the sweep was checked against the ACTUAL
  relocated architecture in the companion charly-repo commit
  (`a5f931b2`, `fix(charly,clean)!: relocate the retention engine into
  candy/plugin-clean`) — every claim in the rewritten paragraphs matches the
  real file layout, capability names, and call graph.
- R5 grep self-test: `git grep -n 'charly/retention\.go\|charly/host_build_retention\.go'`
  across the whole `plugins` tree returns TWO hits, both legitimate
  historical/explanatory context, never a stale live claim: one in
  `CHANGELOG/2026.186.0929.md` (history, exempt by the grep self-test's own
  rule) and one in `core/skills/clean/SKILL.md`'s rewritten paragraph itself
  — "...relocated from the former `charly/retention.go`..." — explicitly
  past-tense, naming WHERE the engine moved FROM, not asserting it is still
  there. `charly/host_build_retention.go` (the file that WAS deleted) has
  zero remaining hits anywhere, live or historical.
- Markdown integrity: no broken headings/tables/links introduced.

Companion PRs: `opencharly/sdk#100` (the retention wire-type schema
extension) and `opencharly/charly` branch `feat/k1-retention-relocation`
(the code relocation itself, targeting `feat/k1-unblock-wave1`).

Assisted-by: Claude Code (Anthropic claude-fable-5; documentation reviewed)

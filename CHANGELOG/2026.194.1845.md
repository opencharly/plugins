# 2026.194.1845 — orchestration findings: ledger discipline, TaskStop semantics, rooting scope-of-validity

Codifies four operator-escalated findings from the wave-3 orchestration into the skills:

- **`/charly-internals:agents` "Migration-ledger discipline" (new subsection):** register
  rows are conflated-until-measured (variances −79%/−45%/~−80%/+60% in four consecutive
  scoping rounds); a measured per-file scoping map precedes every cut; an enabler is named
  by its exact SURFACE (def/fields/op), never a subsystem word, and every row naming an
  enabler is re-audited when it lands; every plan-vs-measured mismatch is a BLOCKING
  incident with a dedicated root-cause-analyzer RCA; a gap inside an assigned cutover
  means BUILD the enabler in the cutover (spike-first, growth-with-consumers) — never
  clean-slice-plus-deferral; exclusions only as per-file boundary-law justifications.
- **`/charly-internals:agents` "Agent lifecycle hygiene":** a `shutdown_request` message
  is NOT a stop — the process and pane persist until `TaskStop(<name>)`; fire it the
  moment work is accepted (the observed leak: 8 finished wave-teammates idling for hours).
- **`/charly-internals:agents` invariant #1 + `/charly-internals:git-workflow` B5
  (claim-keyed sweep, R1):** scope-of-validity correction to the submodule-rooting denial
  claim — USER/MANAGED-level settings (e.g. the operator's `autoMode.allow` rule) resolve
  independently of project root; a submodule-rooted validator under that rule posted
  `success` and merged with zero denials. Superproject rooting remains the rule; diagnose
  a denial by checking both settings layers, never root alone.

*Assisted-by: Claude (documentation reviewed)*

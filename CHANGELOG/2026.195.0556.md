# 2026.195.0501 — docs: validate rule ENGINE moved to candy/plugin-box (docs leg of superproject #60, K3-D+)

Superproject task #60 (K3-D+) moved the `charly box validate` rule ENGINE out of `charly/` core into
the compiled-in `command:box` plugin (`candy/plugin-box`), where it reads the resolved-project envelope.
`charly/validate_check.go` was DELETED and its op-checks moved to `candy/plugin-box/validate_check.go`.
`charly/validate.go` now holds ONLY the host-natural checks that need the raw loader. This is the
PLUGINS-submodule docs leg: sweep the stale R5 skill claims that still located the engine in
`charly/validate.go` / `charly/validate_check.go`. Docs-only; behavior claims (what validate enforces)
are preserved — only LOCATION/ownership is corrected.

- **`internals/skills/go/SKILL.md`** — (1) the `validate.go` file-map row no longer claims "All validation
  rules"; it now lists ONLY the host-natural checks left in core (`validateCandyCUESchemas` /
  `validateProjectCUESchemas` / `validateVocabularyCollections` / `validateBuildAndDistro` /
  `validateBuildTunables` / `validateMergeConfig` / `validateBuilderRefs` / `validateBoxBaseFrom` /
  `validateRemoteCandies` / `boxEntityWireYAML` / `isNodeFormFile`) and states that the per-kind/op/candy/graph
  rule ENGINE (`validateCandyTasks` / `validateCandyContents` / `validateAliases` / `validateVolume` /
  `validateCandyReferences` / the DAGs / `validateOps`) moved to `candy/plugin-box`. (2) The `validate_check.go`
  file-map row is DELETED (the file no longer exists in core). (3) "Add a New Validation Rule" no longer says
  rules are centralized in `charly/validate.go` — host-natural checks go there, the rule engine in
  `candy/plugin-box`. (4) The `/charly-build:validate` cross-ref points `validateCandyTasks` at
  `candy/plugin-box/validate_rules.go`. (5) The `/charly-check:check` cross-ref drops `validate_check.go` from
  the core-file list and notes op-level check validation moved to `candy/plugin-box/validate_check.go`.
- **`internals/skills/generate-source/SKILL.md`** — both `charly/validate.go:validateCandyTasks` references
  (the `Task` struct note + the source-code-map cross-ref) now point at `candy/plugin-box/validate_rules.go:validateCandyTasks`.
- **`build/skills/charly-mcp-cmd/SKILL.md`** — the op-level deploy-scope-enforcement reference now points at
  `candy/plugin-box/validate_check.go` (was bare `validate_check.go` implying core).
- **`check/skills/check/SKILL.md`** — the "Core keeps" implementation-map list drops `validate_check.go` and
  notes the op-level check validation (`validateOps` / `validateCheck`) moved out of core to
  `candy/plugin-box/validate_check.go`.
- **`image/skills/image/SKILL.md`** — the builder-capability-validation source line: `validateBuilders` is
  `candy/plugin-box/validate_graph.go` now (was `charly/validate.go` — it moved with the rule engine).
- **`internals/skills/libvirt-renderer/SKILL.md`** — the candy/image-level libvirt-snippet XML check: the
  `validateLibvirt` rule moved to `candy/plugin-box/validate_rules.go` (was `charly/validate.go`); it invokes
  `sdk/vmshared`'s `ValidateLibvirtSnippet` (which already lived in the vmshared kit, not core).

Noted for a SEPARATE cutover (the K3-A graph fold, task #48 — not this validate leg): `image/skills/image/SKILL.md`
still cites `charly/graph.go` (`ResolveBoxOrder`/`BoxNeedsBuilder`), but `charly/graph.go` is deleted (now
`charly/graph_shim.go` delegating to `sdk/deploykit/graph.go`) — an unrelated stale claim left untouched here.

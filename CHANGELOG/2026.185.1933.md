# Fix stale go-skill recipe-heading cross-references

Three skills (build/validate, image/image, image/layer) cited a
/charly-internals:go recipe named "Updating Go code when an ingress CUE schema
changes" that no longer exists — the actual heading is "How to change the
charly.yml schema (CUE is the single source of truth)". Repointed all three.

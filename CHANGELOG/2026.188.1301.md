# 2026.188.1301 — docs-currency sweep: externalized MCP server + doctor, full hostBuilders roster

Documentation-only cutover (P0 of the core-minimization program): sweep every
skill claim that predated two landed externalizations and one seam-roster
expansion, so the cookbook matches the code again.

## Stale claims fixed (claim-keyed, R1/R5)

- **`charly/mcp_server.go` no longer exists** — the `charly mcp serve` server
  is the `command:mcp` leg of `candy/plugin-mcp` (`serve.go`), consuming the
  hidden `charly __cli-model` host seam (`charly/cli_model_cmd.go` →
  `sdk.CLIModel`); tool calls fork/exec `charly` with a managed project prefix
  (`computeProjectPrefix` + `childCharlyEnv`) — the former in-process capture
  model (`captureAndRun`, `runMu`, os.Stdout redirect) is gone, and charly's
  core links NO MCP SDK. Swept: `/charly-internals:go` (file map row,
  Kong-collision + env-var-proxy insights, cross-refs, source counts),
  `/charly-build:charly-mcp-cmd` (Overview, Architecture, Auto-default,
  examples), `/charly-coder:charly-mcp` (fallback mechanics, cross-refs),
  `/charly-build:new`, `/charly-image:image`. Fragile tool counts ("190
  tools", "63/51 destructive") replaced with count-free phrasing.
- **`charly/doctor.go` no longer exists** — dropped the stale
  "named to avoid collision with doctor.go" rationale in the `checkrun.go`
  row (`/charly-internals:go`); `/charly-core:charly-doctor` was already
  current (candy/plugin-doctor + the hostprobe seam).
- **The generic host-builder registry has TEN kinds**, not the ~6 the skills
  named: `image`, `containerfiles`, `overlay`, `step-emit`, `plugin-binary`,
  `cli`, `hostprobe`, `feature`, `settings`, `retention`. Rosters updated in
  `/charly-internals:plugin` (the F10 HostBuild bullet) and
  `/charly-internals:install-plan` (the `plugin_dispatch_reverse.go` row).
- The env-var-proxy implementation note now points at its one live instance
  (`projectDirPreParse`, `charly/plugin_command_prescan.go`); the deleted
  `bootstrapProject()`/`mcp_serve_default_repo_test.go` references are gone.

R5 grep self-test: `mcp_server.go`, `bootstrapProject`, `captureAndRun`,
`190 tools`, `mcp_serve_default_repo_test`, `doctor.go` return only this
CHANGELOG across `plugins/**/*.md`.

*Assisted-by: Claude (documentation reviewed)*

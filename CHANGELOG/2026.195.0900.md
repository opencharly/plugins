# 2026.195.0900 — vm-lifecycle-correctness docs leg: refresh the stale `charly vm stop` caveat (#77)

The correlated `plugins`-repo leg of the superproject's vm-lifecycle-correctness batch
(opencharly/charly#114). #77 fixed `charly vm stop`'s backend-mismatch false-success
(authoritative `vmHolder` probe + poll-for-shutoff verify), which made the VM skill's
"Known live-tested caveats" entry for `charly vm stop` STALE — R1 makes the documentation
divergence an incident and the sibling-set fix BLOCKING.

## Change

`vm/skills/vm/SKILL.md` — the `### charly vm stop state-sync race under QEMU backend` caveat
(whose "Workaround: proceed to `charly vm destroy`" described the pre-#77 false-success) is
replaced with a present-tense description of the CURRENT behavior: `charly vm stop` tears the
domain down from whichever backend ACTUALLY holds it (the shared `vmHolder` probe, the same
primitive `charly vm destroy` uses, regardless of the configured `vm.backend`) and VERIFIES the
domain is no longer RUNNING before reporting success; a wedged guest that ignores ACPI hard-fails
(never a silent "Stopped VM"), and a genuinely-absent VM errors "no such VM" (mirroring destroy).
The historical bug is named once for context, but the skill describes the current state in present
tense (per the doc-split: skills state current behavior; history lives in the repo's CHANGELOG).

## Tier

`documentation reviewed` — Documentation-only change class (a `*.md` skill edit, zero behavior
change). Non-runtime standards: adversarial consistency review (the new text matches the
#77 fix's actual behavior — authoritative probe + poll verify + nonexistent-error, confirmed
against the superproject's live `charly vm stop` R10), R5 grep self-test (the stale "state-sync
race" / "Workaround: proceed to charly vm destroy" claim is gone from the skill), cross-reference
validation (the new section sits in the "Known live-tested caveats" list alongside the unchanged
neighbors), markdown integrity.

Assisted-by: Claude (documentation reviewed)
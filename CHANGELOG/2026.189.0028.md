# 2026.189.0028 — doc sweep for the P1 dead-code deletions

Companion documentation sweep for the superproject P1 lint/dead/dup cutover
(claim-keyed, R5): the skills no longer reference the deleted core identifiers.

- `replaceOrAppendManagedBlock` / `replaceOrPrependManagedBlock` (deleted from
  `charly/shell_profile.go`; the canonical managed-block splice is kit's
  `sdk/kit/profile.go`): swept from `/charly-internals:install-plan`,
  `/charly-internals:local-infra` (file-map row + finalizer paragraph), and
  `/charly-internals:vm-deploy-target`.
- `registerSubstrateLifecycle` (deleted; wire-backed lifecycle hooks register
  via `registerPluginSubstrateLifecycle` at plugin-load — no compiled-in
  substrate lifecycles exist after M4): the
  `/charly-internals:vm-deploy-target` file-map row now names the live
  registration path.

R5 self-test: the deleted identifiers return only CHANGELOG hits across
`plugins/**/*.md`.

*Assisted-by: Claude (documentation reviewed)*

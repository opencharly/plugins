# 2026.198.1030 — W3 R5 sweep completion: tailscale-up's stale renderBuilderScript location

One-line blocking fix surfaced by charly PR #142's validator (R5 claim-keyed
sweep): `infrastructure/skills/tailscale-up/SKILL.md` still located
`renderBuilderScript` in `charly/deploy_host_helpers.go`; the W3 build-engine
wave relocated it to `sdk/deploykit/localpkg.go` as `RenderBuilderScript`.
Solo landing justified as blocking-class: charly #142 cannot merge while its
pinned plugins main carries a claim the merge itself falsifies.

Assisted-by: Claude Code Anthropic Claude Fable 5 (documentation reviewed)

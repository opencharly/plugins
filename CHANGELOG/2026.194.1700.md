# 2026.194.1700 — P12 plugins-half R5 skill sweep: check-family references → the compiled-in `command:check` plugin

Documentation-only cutover (the plugins half of P12). P12 relocated the entire
`charly check` command family + the AI-iteration harness out of `charly/` core into
the compiled-in plugin candy `candy/plugin-check` (`command:check`); `charly check
<sub>` now dispatches in-proc through the plugin's `Invoke(OpRun)` → its own kong
grammar, and core keeps only the kind-blind host SEAMS + the gathering/engine
mechanisms. This sweep (R5) updates every skill paragraph that still named a
DELETED core file or identifier so the docs describe the new architecture in
present tense.

Deleted core files whose logic moved into `candy/plugin-check/` — `check_loop.go`
(→ `harness_loop.go`), `check_watchdog.go` (→ `watchdog.go`), `check_bed_run.go`'s
`runCheckBed` orchestration (→ `bed_run.go`), plus `check_clone.go` / `check_note.go`
/ `check_synccreds_cmd.go` / `check_runlocal_cmd.go` / `check_runner_stream.go` /
`plugin_command_check.go`. Deleted core identifiers — `CheckCmd` / `CheckBoxCmd` /
`CheckRunCmd` / `CheckFeatureCmd` / the check-run management Cmds / `CheckLiveCmd.Run`
/ `runCheckBed` / `RunHarness` / `ProgressWatchdog` / `commitIterationBestEffort` /
`attachNestedCheckPlugins` / `CheckFailedError` / `CheckCheckFailExitCode`. Core
retains the seams (`host_build_check_bed.go` / `host_build_check_config.go` /
`host_build_check_run.go`), the "score"/"preflight" bodies (`check_runner_live.go`'s
`RunCheckLive`, `check_image_preflight.go`'s `ensureScoreImages`), the CLI-free
live-check gather engine (`check_cmd.go`, carried by the kept `CheckLiveCmd` struct),
`check_runner_cmd.go`'s lone `scorePodTargetEntry`, `check_feature_run.go`, and all
gathering/validation. Exit-code error/exit types are now owned by the sdk
(`sdk.ExitCodeError` + `sdk.CheckFailExitCode` / `sdk.CheckSkippedExitCode`). The
declarative live-container probe verbs (`kube`/`adb`/`libvirt`/…) are UNCHANGED —
dispatched through the provider registry, never the retired `attachNestedCheckPlugins`
path.

**`check/skills/check/SKILL.md`:**

- Bed-runner references (`runCheckBed`) → the check-bed setup op in
  `candy/plugin-check/bed_run.go` over the host `check-bed` session seam
  (`host_build_check_bed.go`); the `CHARLY_REPO_OVERRIDE` mechanism stays in
  `charly/refs.go`.
- Exit-code implementation notes (`CheckFailedError` / `CheckCheckFailExitCode`) →
  the sdk-owned `sdk.ExitCodeError` + `sdk.CheckFailExitCode` /
  `sdk.CheckSkippedExitCode`, mapped by charly's `main()` via `errors.As`.
- Image-preflight wiring (`CheckRunCmd.Run`) → the `host_build_check_run.go`
  "preflight" seam the `command:check` plugin drives for the host-target run.
- Three-primary-modes implementation note (`CheckBoxCmd.Run()` / `CheckLiveCmd.Run()`)
  → the live-gather engine in `check_cmd.go` (the kept `CheckLiveCmd` struct) + the
  `charly check box` flow in `candy/plugin-check`.
- Live-container verb dispatch (`attachNestedCheckPlugins`) → the provider registry
  (no separate in-`charly check` attach path).
- Issue-52328 defense (`check_loop.go`'s `commitIterationBestEffort`) →
  `candy/plugin-check/harness_loop.go`'s per-iteration commit.
- The `/charly-internals:go` implementation-map bullet rewritten to the new
  core-kept-seams vs plugin-CLI split.

**`check/skills/libvirt/SKILL.md`:** the nested-runtime operator-command note drops
`attachNestedCheckPlugins`, pointing at the provider registry.

**`core/skills/clean/SKILL.md`:** the `charly check run` auto-prune hook
(`CheckRunCmd.Run` in `check_runner_cmd.go`) → the `command:check` plugin's harness
over the generic `retention` `HostBuild` seam (retention keys still on `BoxConfig`).

**`internals/skills/go/SKILL.md`:** the Kong branch/leaf note, the `refs.go` /
`checkrun_verbs.go` / `check_members.go` / `bundle_members.go` / `local_image.go`
rows, and the Declarative-Testing table intro updated; the `check_cmd.go` (live
gather engine) and `check_runner_cmd.go` (`scorePodTargetEntry` only) rows rewritten;
the `check_loop.go` and `check_watchdog.go` rows removed (files relocated to
`candy/plugin-check/`); the `check_runner_live.go` row rewritten to the
`host_build_check_run.go` "score" seam.

**`internals/skills/install-plan/SKILL.md`:** the `--dev-local-pkg` bed-image-build
reference (`check_bed_run.go`) → `candy/plugin-check/bed_run.go` over the
`HostBuild("cli")` seam (the bed-vs-production distinction unchanged).

The paired superproject pointer bump rides the same logical cutover.

*Assisted-by: Claude (documentation reviewed)*

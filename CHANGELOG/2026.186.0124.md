# M15 skill sweep: migration engine relocated to candy/plugin-migrate

Claim-keyed R5 sweep for M15 (migrate → compiled-in command:migrate plugin): the migration engine,
table, and CLI moved OUT of charly core into candy/plugin-migrate. Repointed every stale path claim
across build/skills/migrate, internals/skills/{go,cutover-policy,git-workflow}: charly/migrations.cue
→ candy/plugin-migrate/migrations.cue; charly/migrate_engine.go → candy/plugin-migrate/engine.go;
"the TABLE stays in charly" / "runs in CORE" → the compiled-in command:migrate plugin. The
below-floor/behind-head load-gate HINTS stay core (they only point at the command).

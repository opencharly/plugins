# 2026.203.1027 — dead-code radical-removal batch (plugins/docs leg)

Docs-last leg of the dead-code radical-removal batch (companion `sdk` and
superproject PRs delete the actual dead code this fixes a stale reference to).

## Fixed (R1 — documentation divergence)

- **`internals/skills/libvirt-renderer/SKILL.md` "Validation" section** claimed
  `sdk/vmshared`'s `ValidateLibvirtSnippet` was "invoked by the **candy/image-level**
  `validateLibvirt` rule in `candy/plugin-box/validate_rules.go`" — neither the
  rule nor any call to that function exists anywhere in the current tree (a
  fresh `grep -rn` across the whole superproject + all submodules confirms
  zero hits), and the claim directly contradicted the same doc's own preceding
  sentence ("there is no Go libvirt validator"). `ValidateLibvirtSnippet` itself
  is deleted as dead code in the companion `sdk` PR (it was exercised only by
  its own unit test). Rewrote the paragraph to describe the actual current
  state: raw `libvirt.snippets` are modeled typed-open and are not XML-parsed
  at ingress time; well-formedness is caught only indirectly by the egress
  `RenderDomainXML` → `ValidateXMLEgress` path documented immediately below it.

## Change class

Documentation-only — zero behavior change. Confidence tier: `documentation
reviewed`.

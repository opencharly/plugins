# 2026.193.1135 — D3: the pre-PR formatter gate + docstring/comment R5 sweep move into go-quality

Documentation-only cutover (D3, the last memory-only generalizable practice moved to its
committed skill, per the operator directive that memory holds only local-host facts).

**`internals/skills/go-quality/SKILL.md`** — new section "The pre-PR FORMATTER gate + the
docstring/comment R5 sweep": (1) `gofmt -l` AND `golangci-lint fmt --diff` must both be clean
before any Go PR (`golangci-lint run` reports neither formatter-class drift nor gofmt drift —
each cost a real CHANGES-REQUESTED round); (2) the R5 sweep is claim-keyed across DOCSTRINGS
and COMMENTS, word-boundary-grepped with every hit READ — a comment hit blocks like a code hit,
and conversely a comment mention is NOT a live caller (a doc-comment reference once masqueraded
as a caller and nearly kept dead code alive).

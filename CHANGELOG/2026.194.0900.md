# 2026.194.0900 — skills: the VM libvirt domain is keyed by the DEPLOY, not the entity (#33 docs half)

The documentation half of #33 (VM naming: the libvirt domain is keyed by the DEPLOY, not the shared
`kind:vm` entity). A claim-keyed R1/R5 sweep: every skill that asserted the OLD entity-keyed domain
convention is corrected to the per-deploy DOMAIN IDENTITY (`vmshared.VmDomainIdentity`) the code now
uses, so sibling beds referencing one entity get distinct `charly-<deploy>` domains — collision-free by
construction. Documentation-only cutover (no behavior change here — the runtime lives in the
superproject); each claim verified against the landed code, not assumed.

- **`vm/skills/vm/SKILL.md`** — the `charly vm destroy` caveat: the domain is
  `charly-<VmDomainIdentity(deploy)>`, NOT `charly-<vm-entity>`; a `vm:` bed `check-charly-vm`
  (`from: charly-vm`) runs `charly-check-charly-vm` (was wrongly documented as `charly-charly-vm`). The
  deploy path destroys with `charly vm destroy <entity> --domain <deploy>`; a direct create keeps
  `charly-<entity>`; a legacy pre-P33 orphan (`charly-<entity>`) is cleared with the bare form.
- **`check/skills/check/SKILL.md`** — the orphan-cleanup caveat: a bed's domain is `charly-<bed>`, so an
  orphan is torn down with `charly vm destroy <entity> --domain <bed>` (was: "destroy takes the ENTITY
  name" → `charly vm destroy check-charly-vm` targets a nonexistent domain — the inverse of the truth).
- **`check/skills/libvirt/SKILL.md`** (×3) + **`check/skills/spice/SKILL.md`** (×3) — the `libvirt:` /
  `spice:` verb target resolution: the host passes `Runner.vmTargetName()` = the resolved per-deploy
  DOMAIN IDENTITY (not the VM-entity name), so the verb addresses `charly-<deploy>`, distinct per bed.
  (Every "resolved VM-entity name … `charly-<vm-entity>`, not `charly-<deploy-name>`" was inverted.)
- **`internals/skills/agents/SKILL.md`** — the bed-isolation ground-truth: made explicit that a `vm:`
  bed's libvirt domain is keyed by the DEPLOY name (`vmDomainIdentity`), not the shared entity, so
  sibling beds on one entity (`vm: {from: eval-vm}`) get distinct `charly-<bed>` domains + per-deploy
  disk overlays — the concurrency ground-truth #33 makes true by construction.
- **`local/skills/local-deploy/SKILL.md`** — the managed ssh alias is `charly-<VmDomainIdentity(deploy)>`
  (a direct create's deploy name IS the entity; a bed's alias is `charly-<bed>`).
- **`internals/skills/vm-deploy-target/SKILL.md`** — `LifecyclePrepareInput`'s `Alias` / `StateDir` /
  `SSHPort` key off the per-deploy domain identity; `Entity` still names the disk/spec source. The
  `OpArtifactKey` row (candy artifacts + the k3s `ClusterProfile` under `vm:<entity>`) is left as-is —
  DELIBERATELY entity-keyed (one cluster per VM shared by several beds), confirmed against the code.

Assisted-by: Claude (documentation reviewed)

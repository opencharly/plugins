# 2026.197.1904 — Guidance-codification batch: boundary-law trap + alias-residue guard

Documentation-only batch cutover (two files touched in `plugins`, one PR, one
CHANGELOG stamp per the Cutover Sizing Law), landing session-proven
generalizable guidance into the committed skills so no future
session/teammate/validator repeats it. Operator-directed: these insights were
surfacing only in a private per-session memory nobody else reads, which is
itself the knowledge-leak failure mode the skills/SKILL.md "Two-Layer Sync
Architecture" section already names. This is the first (non-conflicting)
pass of a larger guidance batch; two further items — a teammate-misdirection
discipline for `/charly-internals:agents` and a git-mv stale-pathspec guard
for `/charly-internals:git-workflow` — are HELD in the same working tree
until `opencharly/plugins#75` (docs batch 4, in flight on the same two
skills) merges, to avoid a same-file collision; they land as a follow-on
commit on this same branch once that PR is in.

## Member 1 — the named boundary-law trap (`internals/skills/plugin/SKILL.md`)

Two operator-run architecture auditors independently mis-classified the
deploy and check cones as ~90% permanently-core, both using the same excuse:
a construct "can't cross the process boundary" or "drives podman/ssh/flock/
systemd itself," so it must stay in `charly/`. The true kernel in those
cones turned out to be ~0.3%. What let one of them self-correct was a
concrete grep, not the abstract E/M/B/D test alone. Added three new
paragraphs to "The kernel/plugin boundary law" section, immediately after
the existing "un-gameable self-test":

- **The named trap** — calling a construct a "host-boundary object" is NEVER
  a permanence reason; a plugin drives that same host object itself (as
  `candy/plugin-vm` and `candy/plugin-kube` already do) or reaches a live
  venue over the reverse-channel broker (M4) — needing the broker or the
  host IS the seam's whole point, never a reason to stay core.
- **The defines-vs-calls test** — the concrete grep that operationalizes the
  trap: grep for where a mechanism is DEFINED (`provider_registry.go`,
  `materialize.go`, the reverse-channel broker in `plugin_inproc*.go` /
  `plugin_dispatch_reverse.go`, all grep-verified live in `charly/` before
  citing); a file that only CALLS one of those is a capability that USES the
  mechanism, not the mechanism itself — an R-item, it moves. "A capability
  that uses M1–M4 is not thereby M1–M4."
- **The practical audit framing** — invert the default: every file is an
  R-item (moves to a plugin) UNLESS it is literally one of the tiny kernel
  whitelist (the four in-core M-mechanisms, the B bootstrap root, D
  kind-recognition data, or an E generic envelope). Names the bed runner,
  the check/ADE harness, the build engine, the deploy walk, and the VM
  engine as R-items by this test, and cites the ~90%-vs-~0.3% incident as
  the motivating example of the failure this framing forecloses.

CLAUDE.md's "The kernel/plugin boundary law" pillar gets a matching one-line
sharpening (the same named trap, in mandate form) plus an extended
`*Detail:*` pointer to this skill section — the mandate lives in CLAUDE.md,
the full framing and incident stay here (R3 — one shared abstraction, no
duplication).

## Member 2 — generalize-aggressively (`internals/skills/plugin/SKILL.md`)

A second insight from the same audit round: a K-wave move that just
relocates a per-case branch verbatim (rather than collapsing it into a
generic mechanism) under-delivers on the boundary law's own intent. Added a
paragraph immediately after "The canonical shape every kind copies —
resolve-to-envelope," stating that every K-wave move is a GENERALIZATION,
not a mechanical relocation — pointing at R3 and "Prioritize Clean
Architecture" in CLAUDE.md rather than restating their bodies — with the
practical rule of thumb: reach for an existing generic seam first
(`HostBuild`, `InvokeProvider`, `OpResolve`/`OpEmit`, the
`substrateLifecycle` legs), collapse per-case branches into one word-keyed
mechanism (citing the already-landed `OpStatusCollect` generalization as a
real precedent), and delete every duplicate/transitional alias the move
obsoletes rather than re-exporting it.

## Member 3 — alias-residue location guard (`internals/skills/skills/SKILL.md`)

A go-skill row asserting the exact per-symbol file:line of transitional
`charly/*_aliases.go` residue produced 5 alias-attribution errors across 5
validation rounds this session, because the named residue kept moving out
from under the claim. Added a paragraph to the "Skill↔code source-map sync
audit" section (after step 5, "Land docs-only"): a skill MUST NOT assert an
alias's exact location — state the stable spec-source (the owning
plugin/sdk package) and the K5-dissolution destination instead, and
`grep -rn` every concrete alias-attribution claim against the live tree
before asserting it.

## Change class + tier

Documentation-only — 2 files (`internals/skills/plugin/SKILL.md`,
`internals/skills/skills/SKILL.md`) in `plugins` + this CHANGELOG entry, and
a matching one-line sharpening in the superproject's `CLAUDE.md` (landed in
its own superproject commit, gitlink-bumped to this commit once merged).
Zero behavior change, no Go / YAML-schema / hook / workflow code touched.
Gate: the non-runtime standards (adversarial consistency review; markdown
integrity — bold/backtick balance checked per touched hunk; cross-reference
validation — the CLAUDE.md `*Detail:*` pointer resolves against real
sections in this skill; R5 grep self-test — purely additive, no identifier
removed or renamed). Tier: `documentation reviewed`.

*Assisted-by: Claude Code Claude Sonnet 5 (documentation reviewed)*

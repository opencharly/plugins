# 2026.195.0000 — docs: VM extra-port-forward `auto:<guest>` sentinel (rides superproject #65)

Documents the VM extra-port-forward auto-allocation feature (superproject #65) — one additive line each,
folded as the docs leg of that cutover (docs ride their code change).

- **`vm/skills/vms-catalog/SKILL.md`** — the `network.port_forwards` example now shows the `auto:<guest>`
  sentinel: `"<host>:<guest>"` where the host may be `auto` → a free host port auto-allocated at vm-create
  and persisted in `vm_state`, the sibling of `ssh.port_auto`. Use it so concurrent beds sharing one VM
  entity never collide on a hardcoded host port.
- **`internals/skills/agents/SKILL.md`** — the eval-bed concurrency invariant ("EVERY eval bed MUST use
  PORT AUTO-ALLOCATION — never a hardcoded host port") now names the VM `network.port_forwards`
  `auto:<guest>` sentinel alongside `ssh.port_auto` / `port: [auto]`, citing the 16443 collision the
  k3s-vm bed pair hit.

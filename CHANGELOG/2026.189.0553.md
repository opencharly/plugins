# 2026.189.0553 — finalize PR-template single-source + enforce the filled template

Completes the PR-template consolidation in `plugins` and enforces it:
- **Delete `plugins/.github/pull_request_template.md`** — plugins now inherits the
  org-wide default from `opencharly/.github` (the single source bootstrapped this
  session). Part of the R3-at-repo-level consolidation.
- **git-workflow skill** now names `opencharly/.github/.github/PULL_REQUEST_TEMPLATE.md`
  as the single template source (no per-repo copy) at the `gh pr create` step.
- **pr-validator item 1 (P1.15)** strengthened from "empty/template-only body FAILS"
  to a MANDATORY filled-template check: a blank applicable section, a bare checkbox
  with no HOW, or a "will test" promise FAILS. It spells out the required halves —
  the *How R10-tested* block (exact gate + `disposable:true` target + fresh-rebuild/R9
  + changed-path-executed + concurrent-roster evidence + pasted output) and the full
  R0–R10 + pillars rule-compliance section answered HOW / `N/A — reason`. The burden
  is on the PR to supply evidence, never on the validator to infer it.

*Assisted-by: Claude (documentation reviewed)*

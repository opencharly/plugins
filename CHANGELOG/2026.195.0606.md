# 2026.195.0606 — agents SKILL: correct the "distinct beds collision-free" claim for shared fixture images

R1 claim-keyed sweep. The agents SKILL's "distinct beds are collision-free by
construction" claim was FALSIFIED for beds sharing a FIXTURE IMAGE name — two beds
building the same box image from different trees raced the store-global podman tag
namespace (last-write-wins, invisible to both; superproject #75). Three sites
corrected (the B3 partition rule, the load-time isolation invariants, and the
per-worktree-binary scheduling rule) to distinguish a bed's distinct-by-name
`charly-<bed>` DEPLOY resources from the fixture IMAGES it builds — now isolated by a
per-run TAG (`<bed-root>-<runCalver>`, the tag analogue of `vmDomainIdentity`) that a
bed run passes on every `charly box build` + deploy step, restoring
collision-free-by-construction for shared-fixture beds.

*Assisted-by: Claude (documentation reviewed)*

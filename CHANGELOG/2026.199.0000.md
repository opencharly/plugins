# 2026.199.0000 — Kimi harness: `setup kimi` profile + harness-generic rulebook citations

`plugins/setup` gains a third harness: `./setup kimi developer|user|container FAMILY`
(+ `--check` / `--dry-run`). Kimi Code reads the repository-native
`.agents/skills/` symlink tree directly as project-scope skills (on-demand
`Skill` invocation), so the kimi profile syncs and drift-checks the same links
the codex profile owns (the generator is now harness-neutral:
`repo_skill_links`), writes no marketplace document, and prints the new
committed `kimi-user-config.toml` snippet — the canonical permission rules and
repo-guarded hooks for the user-level `~/.kimi-code/config.toml` (Kimi has no
project-level config file), which the operator merges by hand; setup never
installs it. `tests/setup_test.sh` now covers kimi: per-profile skill-link
counts computed from the fixture tree, marketplace/settings/HOME untouched,
consumer-owned links preserved, snippet emission asserted.

Rulebook citations are now harness-generic across the plugin tree: the four
agent briefs (`pr-validator`, `testing-validator`, `check-bed-runner`,
`root-cause-analyzer`) and the skill bodies cited `CLAUDE.md` as the sole
rulebook; they now cite "the project rulebook (`AGENTS.md` / `CLAUDE.md`)",
which carry identical R0–R10 rules. `internals/skills/agents` opens with a
per-harness mechanics matrix (Claude Code / Codex / Kimi: rulebook, skills,
sub-agents, validators, workflows, teams, gates, hook events) and keeps the
Claude primitive detail as the Claude column. `README.md` documents the third
harness. References to `CLAUDE.md` as a concrete document (ownership tables,
protected-main policy) remain.

Assisted-by: Kimi Code Moonshot Kimi K3 (fully tested and validated)

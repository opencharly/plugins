# 2026.220.0721 — Rename the Liberation sidebar claim

Regenerate the docs skill so its published-site table names the manifesto's sidebar entry “Liberation” instead of “Agents only” and its Usage section stays on the installed `charly docs` interface. Code-format the `name:type[:path]` grammar token in the generated config skill so Starlight does not misparse it as nested directives (placeholder — the pr-validator finalizes the CalVer at merge).

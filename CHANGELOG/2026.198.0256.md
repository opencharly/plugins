# 2026.198.0256

## K3 build-engine dissolution: stale effective_version.go reference sweep

Companion docs leg for the K3 build-engine cutover (`opencharly/charly#139`):
`charly/effective_version.go` is deleted (a one-caller shim inlined at its
sole call site), but 3 skills still pointed at it as the `EffectiveVersion`
computation site. Repointed all 3 to `deploykit.ComputeEffectiveVersions`
(the actual sdk function, called inline from `generate.go`'s `NewGenerator`):

- `image/skills/image/SKILL.md` — the `version:` field reference table.
- `internals/skills/capabilities/SKILL.md` — the `Version` OCI-label entry.
- `internals/skills/generate-source/SKILL.md` — the CalVer cache-behavior note.

R5 claim-keyed sweep found no other stale references to this cutover's other
deleted symbols (`box_labels_cmd.go`, the 5 dissolved render-seam methods,
`FeatureStep`) across `plugins/`.

Zero behavior change — documentation only.

Assisted-by: Claude Sonnet 5 (documentation reviewed)

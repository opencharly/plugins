# 2026.189.0940 — allow-list the pr-validator status post (fully autonomous landing)

Completes the merge-authority fix: the pr-validator has TWO classifier-gated writes
— post `charly/claude-validation` and `gh pr merge`. The merge rule landed earlier;
this documents the paired status-post rule `Bash(gh api --method POST repos/opencharly:*)`
(committed in the superproject `.claude/settings.json`), so BOTH writes are
deterministic and validated-PR landing is autonomous end to end.
- `pr-validator.md`: the status post uses `--method POST` (not `-X POST`) so it
  matches the allow-rule prefix; POST-only, so it cannot touch branch protection (PUT).
- `git-workflow.md` B5: replaces the now-stale "status post deliberately NOT
  allow-listed" note with the honest trade-off — validator independence is a
  CONTEXT-level discipline (a fresh sub-agent, never the author, posts it), NOT a
  classifier-enforced identity guarantee; GitHub still gates the merge on the status
  server-side, so an unvalidated PR cannot merge regardless.

*Assisted-by: Claude (documentation reviewed)*

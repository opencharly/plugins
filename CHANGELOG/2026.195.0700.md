# 2026.195.0700 — Docs/lessons batch: vms:→vm: node form, migrate-baseline-reset claim sweep, graph.go cite, orchestration lessons

2026-07-14. One documentation-only thematic batch (the Cutover Sizing Law):
every accumulated process-lesson correction plus task #68, each routed to its
ONE owning skill. Every claim was verified against live reality first (RDD) on a
stamped per-worktree binary; the two R1 divergences below were swept claim-keyed
across ALL skills, not only the surfacing file.

## Task #68 — vms-catalog `vms:` wrapper → current `vm:` node form (R1 divergence)

The vms-catalog examples used a top-level `vms:` map wrapper, which the current
loader **hard-rejects** (`node "vms": no kind discriminator — collections and
plan steps live INLINE in the kind value … run: charly migrate`, empirically
proven). Fixes in `vms-catalog`:

- Every `vms:`-wrapper example rewritten to the name-first `<name>: {vm: {…}}`
  node form (verified: each rewritten example loads clean via `charly box
  validate --dir` on a mktemp fixture).
- Field drift `cpus:` → `cpu:` (the yaml key is singular — `VmSpec.Cpus`,
  schema `cpu?: int`).
- The bootc authoring recipe's `source.image:` → `source.box:` (the bootc
  source field is `box:`, not `image:`).

## Migrate-baseline-reset claim β — swept (R1, blocking)

RDD proved `charly migrate` **no longer harvests** the legacy
`box.bootc`/`box.vm`/`box.libvirt` fields into VM entities: that harvest lived in
the migration chain **removed at the `2026.186.2323` baseline reset** (floor
`2026.174.1100`; the only remaining migration is `compactNodeForm`). A config
still carrying those fields predates the floor and is **unmigratable** — `charly
migrate` refuses it (`predates the supported floor … re-author against the
current schema`) with no filesystem change. The false "migrate converts legacy
box.vm/box.libvirt fields" claim was corrected everywhere it appeared:

- `vms-catalog` "Migration from legacy" section rewritten to the reset reality.
- `vm` skill (the "legacy projects convert in one shot" line).
- `image` skill (3 sites: the VM-fields note, the VmSpec cross-ref, the
  Cross-References bullet).
- `vm-spec` forensic table: framing corrected (migrate does **not** perform the
  mapping any more) + the stale `vms.<name>.*` target column updated to the
  current `<name>.vm.*` shape (and `.cpus` → `.cpu`).
- `libvirt-renderer`: the stale `vms.<name>.libvirt.snippets:` notation →
  `<name>.vm.libvirt.snippets:`.

## `charly/graph.go` deleted-identifier cite — swept (R5)

`charly/graph.go` is deleted; the topological sort (`ResolveBoxOrder`,
`BoxNeedsBuilder`, `ExpandCandy`) lives in `sdk/deploykit/graph.go`, reached via
thin `charly/graph_shim.go` wrappers. Citation fixed in `image`,
`generate-source`, and `go` (the file-map row is now `graph_shim.go`).

## Process lessons → owning skills

- **`agents`** (six lessons): (1) a teammate IDLE notification is turn-boundary
  noise — confirm real inactivity via worktree mtimes before nudging/replacing;
  (2) a bash `run_in_background` child of a teammate is harness-reaped at
  turn-yield — confirm a child's terminal state from its completion signal, never
  its output-file size, and run owned work synchronously; (3) stop ONLY your own
  children by task id — a `pgrep -f`/`pkill -f` substring match cross-kills
  siblings; (4) any agent DECLARES an intended live bed run to the orchestrator
  and waits for a slot (bed-mutex + image-tag collision); (5) reinforced:
  per-worktree binaries via `task build:binary` ONLY (a bare `go build` → version
  `unknown`, a recurred gate defect); (6) a parity/golden/coverage instrument is
  a CLAIM until proven NON-VACUOUS (count the cases it exercises — the 0-fixture
  corpus incident).
- **`git-workflow`** (four lessons): a valid base is an ASSEMBLED PAIR (an sdk
  advance is a consumer base only once its super adaptation merged too); verify
  each Go module by CLASS with `GOWORK=off` (sdk standalone-consumed; charly
  workspace-member; plugins per-module); `calver.sh` derives the stamp from the
  HEAD commit time, so a dirty rebuild keeps the old stamp — prove a fix is in the
  binary with `strings bin/charly | grep <marker>`, never the version; a
  mid-chain sibling merge → `gh pr update-branch` then VERIFY the gitlink resolved
  FORWARD (descendant wins).
- **`check`**: the shared-FIXTURE image-tag collision class — beds building the
  same fixture image tag from different trees race in podman's store-global tag
  namespace; the interim mutex-group rule (declare + serialize), superseded by the
  structural bed-scoped-tag fix (a separate cutover).

## Companion superproject change

A superproject pointer bump to the merged plugins commit (all-documentation), at
the `documentation reviewed` tier.

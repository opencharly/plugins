# 2026.185.0456 — selkies-kde: correct the wl-on-KWin + nested-compositor docs to the proven fix

## Summary

The selkies-kde / wl-verb skills carried claims written during an earlier
wrong-hypothesis phase that the RCA + a live bed have since DISPROVEN. Corrected
across four SKILL.md files to match the shipped code + the passing
`check-selkies-kde-pod` bed.

## Corrections

- **`selkies/skills/selkies-kde-desktop/SKILL.md`** — the launcher runs
  `kwin_wayland --wayland-display wayland-1` DIRECTLY (nested), NOT
  `startplasma-wayland`. Documents the RCA: `startplasma-wayland` in a systemd-less
  pod falls back to the plasma_session shim, which runs KWin HEADLESS (no wayland-1
  connection, no `/dev/dri`) → a uniform black frame (4046-byte PNG); the direct
  `--wayland-display` launch + the `cap_sys_nice` strip (so kwin execs under the pod's
  no-new-privileges) fixed it → real content (45746-byte frame). Both the frontmatter
  and the layer line updated off `startplasma-wayland`.
- **`selkies/skills/wl-tools/SKILL.md` + `check/skills/wl/SKILL.md` +
  `selkies/skills/labwc/SKILL.md`** — the wl verb WORKS on KWin: `plugin-wl` is
  compositor-aware (`detectCompositor`) and routes status / screenshot / window
  management (toplevel/focus/close/fullscreen/minimize/geometry) through `kdotool`,
  fail-fasting only the genuinely-unimplemented wlroots paths (`wtype`/`wl-clipboard`/
  `wlr-randr`) with a clear "unsupported on KWin" error. Removed the now-false claims
  "kdotool can't reach KWin's private dbus bus", "check-selkies-kde-pod authors NO
  wl: probes", "compositor-aware plugin-wl … a separate enhancement, not yet built",
  and the contradictory table rows marking `wtype`/`wl-copy/paste` as working on KWin.

## Proof

`check-selkies-kde-pod` full R10 pass — `wl: status` → `compositor: kwin`
(`wl-verb-dispatches` probe green); source-of-truth cross-checked against
`candy/plugin-wl/methods.go` (the `detectCompositor == "kwin"` guards).

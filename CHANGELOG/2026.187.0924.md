# Skills: the kernel/plugin boundary law + doctrine sharpenings

Documentation-class. Records the plugin-first architecture as durable guidance so
future code applies the same clean boundary.

- **`/charly-internals:plugin`** — the "Remaining program" section is replaced by
  **"The kernel/plugin boundary law"**: the E/M/B/D/R clauses, the decision
  procedure, the un-gameable self-test (the incomplete-seam tells: a `spec.<Kind>`
  field-read, a kind-word `switch`, a per-kind Go map), the canonical
  resolve-to-envelope pattern with `builder` as the worked reference, and the
  whole-subsystem corollary. The authoritative detail for the CLAUDE.md pillar.
- **`/charly-internals:go`** — the schema-change recipe records the architecture
  decision: a new KIND is a plugin (not a `reservedKindHandlers` entry); core
  consumes the resolved envelope, never `spec.<Kind>`. Fixes the stale
  `DataKeys` / `dataKeySet` / `reservedKindHandlers` / `checkKindBijection`
  source-map references (now `authoredOpFieldSet` + `checkKindProviderBijection`)
  and the "`spec.OpFields` carries the externalized verbs" claim.
- **`/charly-internals:strict-policy`** — the spike is HOW-not-whether + a support
  tool never an excuse; the forbidden-excuse catalog joins R2's forbidden
  phrasings.
- **`/charly-internals:install-plan`** + **`/charly-internals:capabilities`** —
  boundary-law pointer notes (the IR/wire replies are the E-envelope; the OCI
  labels are a resolved-envelope contract).
- **`/charly-internals:skills`** — the authoritative-copy registry now names the
  boundary law.

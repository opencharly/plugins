# SDD ownership + the named RDD spike

Docs-only skill cutover accompanying the superproject's new "Schema Driven Design (SDD)" pillar. The go skill gains the owning "Schema Driven Design (SDD)" section — the source → generator → artifact pipeline map (cue:gen / pluginsgen / proto:gen), the schema-derived validation boundaries, the reproducibility gates, and the "Generation coverage — current state" ledger of the deliberate hand-written exceptions — and its frontmatter description now names the `sdk/schema/*.cue` surface it owns. The plugin skill ties the per-plugin CUE-schema contract to the pillar. strict-policy adds "The spike" — RDD's named time-boxed, throwaway investigation instrument (industry term: Spike, software development), the spike/R10 boundary, and the schema-spike variant. The skills registry records `/charly-internals:go` as the SDD-operationalization owner and extends the CLAUDE.md canonical-definition exceptions to RDD/ADE/SDD.

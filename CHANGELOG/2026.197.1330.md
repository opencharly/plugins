# 2026.197.1330 — Plugins docs batch 4: P12a go-skill source-map corrections (HELD)

Documentation-only batch cutover, nine members, one PR — **written and committed
now, but the PR is HELD and will not open until the P12a check/ADE
architecture-fold cutover has actually merged** (`opencharly/sdk#59` +
its companion `opencharly/charly` PR on branch `feat/p12a-check-fold`, both
open/unmerged as of this writing). Landing this batch's file-relocation
claims before that cutover merges would describe files that do not yet
exist upstream — the exact class of documentation-reality divergence this
project's R1/R5 discipline exists to prevent, inverted: instead of a stale
doc lagging behind the code, it would be a doc racing AHEAD of code that
might still change shape before it lands. See the git-workflow B2
"producer PR must be merged before consumer's pointer bump" principle,
applied here to documentation rather than a gitlink bump.

**Verification performed before writing a single line:** every file-existence
and relocation claim below was checked directly against the real
`opencharly/charly` and `opencharly/sdk` `origin/main` refs (`git cat-file
-e`, `git show`, `git diff --stat` against the pushed-but-unopened
`feat/p12a-check-fold` branch, and the full `opencharly/sdk#59` PR body) —
never taken on a relayed claim alone (RDD — never trust, verify, extended to
a teammate's confirmed findings). Two findings changed this batch's scope
from what was originally relayed:

- **The `cdp_preresolve.go` stale row no longer exists.** It was already
  corrected in a prior cutover (unrelated to this session's later docs
  batches) — the current `internals/skills/go/SKILL.md` already phrases the
  `candy/plugin-substrate/status_probes.go` row correctly in the past tense
  ("moved from core"). No `retains only` phrasing exists anywhere in the
  repo. Nothing to fix; dropped from this batch.
- **The claimed P14-closeout relocation (`box_labels_cmd.go` →
  `candy/plugin-box`, `labelset.go` deleted) has NO plugins-skill fallout at
  all** — a whole-repo grep for both filenames across every `plugins/*.md`
  returns zero matches. There is nothing stale to correct for this item;
  dropped from this batch (not a "someday", genuinely nothing to do).

## Member 1 — the `checkspec.go` row (`internals/skills/go/SKILL.md`), safe to land NOW

This item is INDEPENDENT of the P12a wave and already true on `origin/main`
today — verified directly against the live `charly/checkspec.go` file. The
row was stale in MORE ways than originally flagged: not only does the
`${NAME[:arg]}` variable-expansion grammar (`ExpandTestVars` / `TestVarRefs`
/ `IsRuntimeOnlyVar` / `RuntimeOnlyVarPrefixes` / `ExpandOpVars`, formerly
`Op.ExpandVars`) now live ONCE in `sdk/kit/checkvars_expand.go` (the
originally-flagged claim), but `Matcher` / `MatcherList` are also
`sdk/spec/union_types.go` defs, not `checkspec.go` (see the round-3 fix
below for the precise per-symbol alias attribution — this early note
undercounted which of the two symbols `sdk/matchers.go` actually aliases).
Rewrote the row to describe what `checkspec.go` ACTUALLY
holds now (`VerbCatalog`, `VerbSpec`, `DoMode`, the do-mode resolvers
`opActsInBuildDeploy`/`opEffectiveDo`/`opEffectiveContexts`/`opInContext`)
and repointed every moved symbol to its real, verified current home. Also
fixed the adjacent `check_members.go` row's `runtimeOnlyVarPrefixes`
citation to `kit.RuntimeOnlyVarPrefixes` (`sdk/kit/checkvars_expand.go`) —
the same underlying claim, caught while already editing this exact area
(R1/R2 — fix what you find while there, the same discipline as batch 3's
"four→six lessons" header catch).

**Round-1 validator FAIL, round-2 fix.** The first pushed revision of this
row ALSO claimed "`Op` itself is `vmshared.Op` (aliased from `spec.Op` in
`charly/vmshared_aliases.go`…)" — the fresh validator (V-133) independently
re-derived this against `origin/main` and found it factually wrong:
`charly/vmshared_aliases.go` contains zero mention of `Op` (confirmed by
reading the whole file), `checkspec.go` does not import `vmshared` at all
(it uses `spec.Op` fully-qualified throughout, including in its own
`opActsInBuildDeploy`/`opEffectiveDo`/`opEffectiveContexts`/`opInContext`),
and the real `Op = spec.Op` alias lives ONLY at `sdk/vmshared/spec_aliases.go`
— the SDK's own internal binding for the `vmshared` package's use, a
similarly-named but DIFFERENT file from `charly/vmshared_aliases.go`. Likely
cause: confusing the two files by name alone. Independently re-verified the
finding before fixing (never-trust-verify extends to a validator's own
finding, not just the author's original claim), then, per the validator's
own instruction to sanity-check the rest of the row for the same mix-up
class, found a SECOND inaccuracy: the row claimed `charly/kit_aliases.go`
"carries the package-main bindings" for the WHOLE variable-expansion-grammar
symbol list, but a direct read of `kit_aliases.go` shows only `TestVarRefs`
and `IsRuntimeOnlyVar` are actually aliased there — `ExpandTestVars` and
`ExpandOpVars` are explicitly documented in that file's own comment as
NOT aliased (no charly-core caller, or test-only caller using `kit.`
directly), and `RuntimeOnlyVarPrefixes` is not referenced in `charly/*.go`
at all. Fixed both in the SAME correction pass, in the SAME appended
commit.

**Round-3: V-133 found a THIRD alias error (same class) — converged with a
comprehensive one-pass audit instead of a fourth single-clause round.** The
round-2 fix's own `Matcher`/`MatcherList` clause claimed both were "aliased
package-wide via `sdk/matchers.go`" — HALF wrong: `sdk/matchers.go` aliases
only `Matcher` (`type Matcher = spec.Matcher`, grep-confirmed); `grep -n
'MatcherList' sdk/matchers.go` returns exactly ONE hit, a comment explaining
why `MatchAll` takes `[]Matcher` rather than `MatcherList` — no type or
alias DECLARATION, just the name mentioned in passing (caught this
imprecision myself while re-confirming the fix fresh, before pasting it as
evidence — a grep returning a comment hit is not the same claim as "zero
mentions"). `MatcherList` is aliased
only at `sdk/vmshared/spec_aliases.go` (same alias block as the already-fixed
`Op`, one line below it). Rather than push a fourth single-clause fix, this
round did a full alias-attribution AUDIT of every claim the row makes, plus
the operator-named sibling lines carrying the same "aliased via" phrasing
class elsewhere in this file (the ones this PR already touches, so their
same-class claims are in R5-sweep scope regardless of exact line number —
the method throughout: `grep -n '<Symbol>\s*=' <the named alias file>` to
CONFIRM before asserting, never restate a citation unverified):

- **The "alias surface" bullet** (`internals/skills/go/SKILL.md`, both its
  first statement and its restatement in the field-parity section) claimed
  "every package-main param type is a `type X = spec.X` alias… defined in
  `sdk/vmshared/spec_aliases.go` and re-exported into package main by
  `charly/vmshared_aliases.go`", naming `BoxConfig`, `Op`, `CandyYAML`,
  `VmSpec`, `ServiceEntry` as examples. Grep-checked EACH against the real
  `charly/vmshared_aliases.go` (`git show origin/main:charly/vmshared_aliases.go`,
  read in full, 103 lines): only `VmSpec` is actually there. `BoxConfig` /
  `Op` / `CandyYAML` / `ServiceEntry` have NO charly-core alias at all — a
  repo-wide `git grep -n 'BoxConfig *=\|CandyYAML *=\|ServiceEntry *='` over
  `charly/*.go` returns nothing, and their real usage sites
  (`charly/build.go`, `charly/layers.go`, …) all reference `spec.BoxConfig`
  / `spec.ServiceEntry` fully-qualified. This describes a PRE-migration
  state — the wave-2 `vmshared`→`spec` envelope-route repoint (`#128`, 23
  symbols, ~200 files) moved most authored types to direct `spec.X` use;
  only a small VM/cloud-init-specific subset (`VmSpec`, `VmSource`,
  `AliasYAML`, `BuilderDef`, … ~34 names, the file's real full content) still
  routes through the real alias chain. Rewrote both instances of this claim
  to state the CURRENT split accurately, preserving the still-valid
  conclusion ("the Go compiler IS the field-parity check" — true either way,
  since both a direct `spec.X` reference and a genuine alias fail the build
  on a field rename).
- **The `layers.go` row's `CandyYAML` citation** claimed "see
  `spec_aliases.go`" for the `CandyYAML = spec.Candy` identity. The identity
  itself is correct (confirmed: `sdk/spec/charly_names.go:25: CandyYAML =
  Candy`, and `Candy` is the real generated struct at
  `sdk/spec/cue_types_gen.go:1733`) but the FILE pointer was wrong — this
  alias lives at `sdk/spec/charly_names.go` (an INTRA-`spec`-package alias),
  never at any `spec_aliases.go` (a different, similarly-named file in a
  different package). Fixed the citation.
- **The `unified.go` VM-support paragraph's closing sentence** ("`VmSpec =
  spec.Vm` is the generated param alias") is wrong on the target type:
  package main's `VmSpec` resolves via `charly/vmshared_aliases.go` →
  `vmshared.VmSpec`, and `sdk/vmshared/spec_aliases.go:167` aliases THAT to
  `spec.ResolvedVm` — a separate wire-envelope type (`sdk/spec/vm_wire.go`)
  that mirrors `spec.Vm`'s fields but is not `spec.Vm` itself. While
  verifying this, also found the SURROUNDING sentence stale in an adjacent,
  non-alias way: `uf.VM` is declared `map[string]json.RawMessage`
  (`charly/unified.go:96`), not `map[string]*VmSpec` — `foldSubstrateKind`'s
  template arm decodes a bare `vm:` node via `decodeStandaloneTemplateJSON`,
  and the plugin's reply is folded back via `foldStandaloneTemplateReply` →
  `foldOpaqueTemplateReply`, which stores the reply JSON VERBATIM (opaque,
  not typed) — the same de-typed pattern documented for `local`/`android`.
  The `decodeNodeValue(gn, &VmSpec)` comparison a test comment cites is
  explicitly labeled "the former" path there (an RDD byte-equivalence
  historical reference, not the current production path). Corrected the
  whole sentence to describe the real opaque-fold flow and the real
  `VmSpec` target type.
- **Verified as ACCURATE, no fix needed:** the `gpu_shim.go` row's `type
  VFIOReport = spec.VFIOReport` claim (confirmed at
  `charly/gpu_shim.go:59`, inside a `type (...)` block); the "sdk/spec —
  generated param structs" bullet's softer, hedged claim ("charly core
  aliases the types via its SCATTERED alias files") — this one does not
  assert "every type" the way the now-fixed "alias surface" bullet did, and
  the alias files it names genuinely exist and genuinely alias SOME types,
  so it was left as-is.

Every fix above lands in THIS SAME commit.

**Round-4: a FIFTH error of the identical class, and the ROOT-CAUSE fix —
de-fragilize the whole claim family instead of auditing it symbol-by-symbol
forever.** V-133 found yet another instance: the `checkvars.go` row (Member
2) claimed `ContainerInspection`/`CheckVarResolver`/`ResolveCheckVarsBuild`/
`ResolveCheckVarsRuntime` are "aliased in `charly/kit_aliases.go`" — that
file has ZERO mentions of any of them. Five errors, three rounds, two rows —
this is SYSTEMIC, not a typo, and continuing to fix ONE symbol at a time
would only find a sixth, seventh, … next round.

**Root cause, per operator directive:** the go skill was asserting EXACT
per-symbol locations in `charly/*_aliases.go` files — but those files are
TRANSITIONAL K5 residue actively being DELETED (ZERO-ALIASES is the
project's v2 target; every `charly/*_aliases.go` exits at K3/K4/K5 as its
call sites move into plugins — see CLAUDE.md "Core is a PLUGIN HOST" +
`/charly-internals:plugin` "Target architecture (v2)"). Documenting the
exact contents of a crutch that is disappearing is fragile BY CONSTRUCTION
(the `#128` `vmshared`→`spec` repoint already invalidated many such claims;
K4/K5 will invalidate the rest) — and it documents the wrong thing anyway,
since the skill's actual job is describing where the STABLE code lives
(`sdk/kit`, `sdk/vmshared`, `sdk/spec`), not which dying core file currently
re-exports which subset of it.

**The fix — one comprehensive pass over the WHOLE file, not another
single-symbol patch:**

1. Fixed the 5th error: the `checkvars.go` row's `charly/kit_aliases.go`
   claim removed, replaced with a hedge (K4/K5 dissolving inventory, grep
   the actual file rather than citing this skill).
2. Enumerated EVERY alias-attribution claim in `internals/skills/go/SKILL.md`
   (`grep -n 'charly/kit_aliases\.go\|charly/vmshared_aliases\.go'` +
   `grep -n 'aliased in\|carries the package-main bindings\|aliased
   package-wide'`) and classified each:
   - **STABLE, sdk-sourced, grep-verified → KEPT**: `Op = spec.Op` at
     `sdk/vmshared/spec_aliases.go` (charly core has NO `Op` alias at all —
     a stable NEGATIVE fact); `Matcher = spec.Matcher` at `sdk/matchers.go`;
     `MatcherList = spec.MatcherList` at `sdk/vmshared/spec_aliases.go`;
     `CandyYAML = Candy` at `sdk/spec/charly_names.go`; `VmSpec`'s target
     type `spec.ResolvedVm` at `sdk/vmshared/spec_aliases.go:167`. None of
     these name a *charly-core* file as the source of truth for a symbol's
     CURRENT existence — they name STABLE sdk-module files, which are not
     K4/K5 deletion targets.
   - **FRAGILE, naming an exact charly/*_aliases.go per-symbol claim →
     DE-FRAGILIZED.** Every instance rewritten to drop the "symbol X lives
     in charly-file Y" assertion, replaced with: the type is spec-sourced
     (stable); charly's OWN `charly/*_aliases.go` re-exports are K4/K5
     migration inventory being DISSOLVED; this skill does NOT enumerate
     which symbol currently lives in which file (that membership changes
     every wave); grep the actual file before citing a specific binding.
     Rewritten locations: the `sdk/spec` intro bullet (line ~88); the
     "alias surface" bullet's exact ~34-name list and file-attribution
     (line ~97 — also dropped the stale exact symbol COUNT, itself a
     fragile commitment); the `checkspec.go` row's `kit_aliases.go` clause;
     the `checkvars.go` row's `kit_aliases.go` clause (the 5th error
     itself); the `checkrun.go` row's `kit_aliases.go` mention; the
     `unified.go` VM-support paragraph's `VmSpec` routing claim; the
     field-parity section's restatement of the alias surface.
3. Every remaining concrete claim was grep-verified BEFORE being kept (the
   rule that would have prevented all 5 errors, stated explicitly in the
   go skill's own new hedge text: "grep the actual file rather than citing
   this skill").
4. Fixed the CHANGELOG's own earlier narrative to match (this entry).

This converges the PR — no per-symbol charly-alias claim remains that could
be wrong about a symbol's CURRENT location, because the skill no longer
makes that class of claim at all. It also aligns the skill with the v2
target: a document that asserted dying residue's exact shape was itself
fighting the direction of travel.

**This member's content is independently true today and could land on its
own** — it is bundled into this held batch only because it is thematically
identical (go-skill source-map staleness) and the Cutover Sizing Law prefers
one landing ceremony per theme over a solo PR for a small fix, per the
orchestrator's explicit sequencing call.

## Members 2–5 — the P12a-pending rows, prepared in advance (HELD content)

Written against the VERIFIED real diff of the unmerged `feat/p12a-check-fold`
branch + the `opencharly/sdk#59` PR body (not guessed) — accurate for the
state that WILL exist once both legs merge, but describing files that do
not exist on any `origin/main` yet. **Do not open this PR before that
merge actually lands** (see the header note above).

- **Member 2** (`internals/skills/go/SKILL.md`): repointed the `checkvars.go`
  row to `sdk/kit/checkvars.go` (`ContainerInspection`, `CheckVarResolver`,
  `ResolveCheckVarsBuild`/`ResolveCheckVarsRuntime`), the `local_image.go`
  row to `sdk/kit/local_image.go` (`ResolveLocalImageRef`, renamed from
  `resolveLocalImageRef`, plus its sibling exports), fixed the `checkrun.go`
  row's `description_run.go` mention (dissolved — callers invoke
  `kit.RunPlan` directly), and dropped `checkvars.go` / `local_image.go`
  from the `/charly-check:check` cross-reference list (replaced with their
  new `sdk/kit` locations).
- **Member 3** (`check/skills/check/SKILL.md`): repointed
  `check_feature_grader.go` → `sdk/kit/grader.go` in the Implementation
  paragraph (also corrected `RunAIOnce` → `RunAgentOnce` — the real current
  function name on `origin/main` today, an unrelated pre-existing staleness
  caught while editing this exact sentence, same R1/R2 in-place-fix
  discipline as Member 1), fixed the `description_run.go` /
  `charly/description_run.go` mention (dissolved, `kit.RunPlan` direct), and
  dropped `checkvars.go` from the `/charly-internals:go` cross-reference
  list.
- **Member 4** (`coder/skills/charly-mcp/SKILL.md`): repointed
  `charly/checkvars.go` `IsHostNetworked()` → `sdk/kit/checkvars.go`
  `ContainerInspection.IsHostNetworked()`.
- **Member 5** (`build/skills/build/SKILL.md` +
  `internals/skills/generate-source/SKILL.md`): repointed both
  `local_image.go` mentions → `sdk/kit/local_image.go`,
  `kit.ResolveLocalImageRef`. The `generate-source/SKILL.md` reference was
  found during this batch's own whole-repo grep sweep — not in the
  originally-relayed list — a claim-keyed sweep catch (R5: the sweep is
  claim-keyed, not just the files named in the initial report).
- **Member 6** (`internals/skills/go/SKILL.md`), the `vnc_helpers.go` and
  `check_members.go` rows — a SECOND, LATER P12a follow-up unit (the pure-
  helper extraction tier), relayed after Members 1–5 were already committed.
  **This member is held to an even stricter verification bar than Members
  2–5: at the time of writing, this follow-up unit exists ONLY as
  UNCOMMITTED, UNPUSHED working-tree changes in a shared local worktree** —
  it has not even reached the `feat/p12a-check-fold` branch that Members
  2–5 describe. Before writing a single word, the actual uncommitted diff
  was read directly (`git diff` inside the worktree, both the `charly/`
  side and the `sdk/` submodule side) rather than trusting the relayed
  description, confirming the precise new symbol names: `sdk/kit/vnc_bridge.go`
  (`kit.UnixToTCPBridge`, moved from `charly/vnc_helpers.go`'s
  `unixToTcpBridge` — `vnc_helpers.go` now holds only `resolveVNCPassword`)
  and `sdk/kit/hostrefs.go` (`kit.CollectHostRefs`, `kit.SplitHostKey`,
  `kit.CloseHostCleanups`, moved from the matching unexported
  `charly/check_members.go` functions — that file's own callers stay core).
  The relayed claim that `applyHostVars` is ALSO stale was checked and found
  to be a separate, PRE-EXISTING divergence unrelated to any P12a work (the
  identifier does not exist anywhere in the current codebase, confirmed by
  a repo-wide grep) — already correctly absent from the `check_members.go`
  row today, so no further action was needed for it. **Because this content
  describes uncommitted WIP that could still change shape before it is
  actually committed and pushed, it gets the SAME final freshness
  re-verification as the rest of this batch before the PR opens — but
  doubly so**: re-confirm the symbol names against the ACTUAL committed/
  merged code, not just against the snapshot read for this entry.

## Member 7 — a new proven lesson: merged `CHANGELOG` files are IMMUTABLE (`internals/skills/git-workflow/SKILL.md`)

A fresh `pr-validator` FAILED an unrelated follow-up PR this session for folding
+60 lines of new narrative into an already-merged, already-tagged
`CHANGELOG/<CalVer>.md` — an understandable reconciliation instinct after a
mid-flight `main` merge (the follow-up PR's author wanted its own related work
documented alongside the original entry), but the wrong direction: a merged
CHANGELOG file is closed history, exactly like the tag sharing its CalVer, and
editing it after the fact would have created a permanent filename↔tag
divergence the moment it landed. The validator caught it before merge.

Added a new paragraph to the "CalVer — generated AND rewritten at MERGE, by the
evaluator" section (right after the existing "ONE fresh stamp per merge,
immutable" paragraph, the natural anchor since it already discusses tag
immutability): a merged `CHANGELOG/<CalVer>.md` is immutable; follow-up work in
the SAME theme, branch, or session never appends to or edits it, and instead
writes its OWN new placeholder entry for its OWN evaluator to stamp at its OWN
merge. This is this batch's own SEVENTH member — landed here rather than as a
solo PR, per the Cutover Sizing Law, since batch 4 is already open and held for
an unrelated reason and this is a small, independent, immediately-true addition
(it does not depend on the P12a wave's merge timing at all).

## Member 8 — validator/worktree coordination insight (`internals/skills/agents/SKILL.md`)

A new proven lesson (motivating incident: PR `#133`) added to the "Sub-agent
operational invariants" area, right after "Stop ONLY your own children, by task
id": a validator worktree (`charly-wt-v133`) was cleanly `git worktree remove`d
by the merge/cleanup flow WHILE an agent's self-model still believed it was
mid-validation — the merge had actually completed 79s earlier (confirmed on
live GitHub). Earlier "uncommitted drift" and "a revert with zero reflog ops"
symptoms in the same tree were the SAME collision under a different name: two
actors sharing one worktree path, one's `git checkout --`/`git clean`
clobbering the other's in-flight edits. Codified as a new subsection,
"Worktree lifecycle + validator identity — orchestrator-owned, never assumed",
stating three rules: (1) worktree lifecycle is orchestrator-owned — a
validator/teammate never removes a worktree it doesn't exclusively own, and
every validator gets a UNIQUE worktree path, never a shared per-PR path a
sibling could remove; (2) one validator identity per PR — never re-brief or
re-spawn a validator into re-validating an already-merged PR; (3) the
orchestrator confirms merge state from LIVE GitHub
(`gh pr view <n> --json state,mergeCommit,mergedAt`) before treating any
"still validating" report as actionable — a desynced self-model is not ground
truth. Landed here (rather than the next docs batch, as originally planned)
because this PR was already being re-opened for the trailer-form reconciliation
below — folding it in is free, same repo, same theme, per the Cutover Sizing
Law.

## Member 9 — trailer-form reconciliation (`internals/agents/pr-validator.md` + `internals/skills/git-workflow/SKILL.md`)

**A correction to this PR's own earlier work, caught by an operator ruling
before merge.** `opencharly/plugins#73` ("docs: require immutable validator
provisioning", merged `a510485`) changed the canonical `Assisted-by` trailer
form — correctly unifying every harness onto ONE generic shape (retiring the
old Claude-only `Claude (<tier>)` shorthand) — but with the WRONG punctuation:
`<Harness> (<Provider Full Model Name>; <confidence>)` (parens-and-semicolon).
When this batch rebased onto `a510485`, its own commit adopted that same wrong
form for its trailer (`Claude Code (Claude Sonnet 5; documentation
reviewed)`), assuming it was already-canonical. **The operator ruling
corrected this**: the true canonical form (which `CLAUDE.md` already carried
and did not need to change) is `<Harness> <Full Model Name> (<confidence>)` —
e.g. `Codex OpenAI GPT-5.6 Sol (documentation reviewed)`,
`Claude Code Claude Sonnet 5 (documentation reviewed)`. The UNIFICATION from
`#73` is correct and stays; only the form's punctuation/order was wrong.

Swept claim-keyed (R5) across every occurrence the semicolon form introduced —
`internals/agents/pr-validator.md` (5 occurrences: the attribution-tier
checklist item, the verdict template's trailer line, the "attribute the
comment" instruction, the Phase-3 squash-merge-trailer instruction, and the
Output Format's PR-comment-posted line) and `internals/skills/git-workflow/SKILL.md`
(3 occurrences: the B1 Step-1 example commit, the worked commit-message
example — `Assisted-by: Codex OpenAI GPT-5.6 Sol (fully tested and
validated)` — and the prose stating the enforced form) — confirmed via
`git grep` that the semicolon form now returns zero non-CHANGELOG hits
anywhere in the repo. `#73`'s other content (immutable validator
provisioning, the removed stale-attribution-regex claim, the
already-initialized-submodules-only worktree refresh) is untouched.

## Change class + tier

Documentation-only — 8 files (`internals/skills/go/SKILL.md`,
`check/skills/check/SKILL.md`, `coder/skills/charly-mcp/SKILL.md`,
`build/skills/build/SKILL.md`, `internals/skills/generate-source/SKILL.md`,
`internals/skills/git-workflow/SKILL.md`, `internals/skills/agents/SKILL.md`,
`internals/agents/pr-validator.md`) + this CHANGELOG entry, zero behavior
change. Gate: the non-runtime standards (adversarial consistency review;
markdown integrity — bold/backtick balance, no tabs, no trailing whitespace,
checked file-by-file; cross-reference validation; the R5 grep self-test for
the trailer-form sweep). Tier: `documentation reviewed`.

**Held-then-released, then amended FOUR times: once for a blocking
correction, then THREE successive validator-FAIL rounds on the identical
"per-symbol charly-alias-file" claim class — the last of which converged
it with a root-cause de-fragilization rather than a fourth single-symbol
patch.** This batch was originally written and
committed while the P12a wave (`opencharly/sdk#59` + its charly companion,
wave-2 `#133`) was still open, and every relocation claim was verified
against the real (if then-unmerged) branch content rather than assumed.
Both legs have since MERGED — `opencharly/sdk` main `6295820`,
`opencharly/charly` main `59bb902` (tag `v2026.197.1631`) — and every
file-relocation claim in Members 1–7 was RE-VERIFIED, fresh, against the
now-real merged code immediately before this PR opened; two
originally-scoped items were dropped as non-issues (see the header note
above). The branch was rebased onto plugins main `a510485` ("docs: require
immutable validator provisioning"), which merged mid-flight and touched the
same `git-workflow/SKILL.md` file — a clean rebase, no conflicts. That
commit introduced the WRONG trailer-form variant (semicolon-punctuated)
across several plugins surfaces, which this PR's first pushed revision
briefly inherited for its own trailer; an operator ruling caught it before
merge and a second commit reconciled it (Member 9) while folding in one
more independently-timed insight (Member 8) per the Cutover Sizing Law —
one landing ceremony, not three. A FRESH `pr-validator` (V-133) then
independently re-derived every claim in the amended PR against the real
merged code and found Member 1's `Op`-alias aside factually wrong (see
Member 1's own "Round-1 validator FAIL" note above) — the ONE blocking
finding out of the whole 9-member batch; every other claim, including the
trailer-form reconciliation's own R5 self-test, was independently confirmed
accurate by that same validator pass. Fixed in a THIRD appended commit.
V-133 then found a SECOND alias error of the SAME class (the
`Matcher`/`MatcherList` clause the round-2 fix itself introduced) — fixed in
a FOURTH appended commit alongside a first pass at auditing every
same-class claim in the file (see Member 1's own "Round-3" note above).
V-133 then found a FIFTH error of the identical class in a DIFFERENT row
(`checkvars.go`'s `charly/kit_aliases.go` claim) — five errors, three
validator rounds, two rows was the signal this was systemic, not a typo.
Per operator directive, the FIFTH appended commit did the ROOT-CAUSE fix
instead of a sixth single-symbol patch: de-fragilized every per-symbol
"aliased in `charly/<file>_aliases.go`" claim across the whole file,
replacing each with the stable spec/sdk-sourced fact plus an explicit
grep-before-citing hedge, since those `charly/*_aliases.go` files are K4/K5
migration residue actively being dissolved (see Member 1's own "Round-4"
note above for the full enumerated audit + what was kept vs. rewritten).
Never a force-push, never a close-and-recreate, at any round.

*Assisted-by: Claude Code Claude Sonnet 5 (documentation reviewed)*

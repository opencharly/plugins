# 2026.195.9999 — #67 doc-sweep — the render-DRIVE move reflected in the skills

#67 render α-fold: the Containerfile render DRIVE moved out of `charly/` core into `sdk/deploykit` + the
compiled-in `candy/plugin-build` (driven over the resolved-project envelope + the new
`HostBuild("render-seam")` reverse leg); the host box-build render-leg is deleted. The skills that named
the pre-move state are swept (R1 documentation-divergence-is-an-incident).

- `internals/skills/plugin/SKILL.md` — the `build` class section: the host-builder is `build-prep` (renamed
  from `build-resolve`); the render DRIVE is in `sdk/deploykit` (driven by plugin-build over the envelope +
  the `render-seam` reverse legs); the host keeps ONLY the loader + build-prep + the resolve-project
  envelope projection. The host-builder KINDS list is `build-prep` / `render-seam` / `merge`.
- `internals/skills/install-plan/SKILL.md` — the `HostBuild` reverse-leg table: `build-resolve` →
  `build-prep` + `render-seam` (ELEVEN registered kinds); the "RESOLVE/RENDER engine is in core TODAY"
  claim updated (the render DRIVE moved to `sdk/deploykit` in #67); the `step-emit` + `pod`-overlay
  sibling references updated to `build-prep`.

# 2026.204.1500 — K5-B validation-correctness batch: shell-overlay field-deletion doc fix

Docs-only follow-on to the prior `architecture-docs divergence batch`
(`CHANGELOG/2026.203.1426.md`), which correctly flagged the deploy-scope
`shell:` overlay's merger (`MergeDeployShell`) as dead code but described the
FIELD itself (`#Deploy.shell`/`#DeployShellOverlay`) as still authorable
("parsed but not applied"). The companion `opencharly/sdk#93` +
`opencharly/charly#182` (K5-B validation-correctness batch, both merged)
went one step further and deleted the field from the schema outright — a
config still carrying it is now migrated away by `charly migrate`'s new
`strip-deploy-shell-overlay` step, and authoring it fresh is a hard load
error (pending `charly migrate`).

## What was stale, and what's actually true now

| Stale claim | Current reality |
|---|---|
| `internals/skills/capabilities/SKILL.md`: the deploy-scope `shell:` overlay is "parsed (`DeployShellOverlay`) but not currently applied to the label" | `#Deploy.shell`/`#DeployShellOverlay` no longer exist in `sdk/schema/deploy.cue` at all — the field cannot be authored or parsed anymore, not merely "parsed but unapplied". The `LabelShell` `Deploy` section stays in the wire shape (a stable three-section shape a future, properly-designed feature could still populate) but is now PERMANENTLY empty. |
| `internals/skills/install-plan/SKILL.md`: "a deploy-scope `shell:` block parses but does not currently overlay onto the label" | Same correction — the field is deleted, not merely unwired. |

Both entries updated to state the field's actual deletion, cite the retiring
schema change (`sdk/schema/deploy.cue`) and the migration step
(`strip-deploy-shell-overlay`), and stop describing a transitional
"exists-but-unused" state that no longer exists.

## Verification

- Non-runtime standards only (this is the Documentation-only change class):
  adversarial consistency review against the merged `opencharly/sdk#93` +
  `opencharly/charly#182` diffs (both independently confirmed merged before
  this doc fix was authored); R5 grep self-test — `git grep
  'DeployShellOverlay\|MergeDeployShell\|shellOverlayToEntry\|replaceShellEntryByID'`
  in this repo returns zero hits outside `CHANGELOG/` and the one
  historical-context mention of the retired CUE def name in each of the two
  fixed skill files (naming what was deleted, for the reader's benefit —
  the same precedent this repo's own prior schema-cutover docs use);
  cross-reference validation (both fixed lines still correctly point at
  `/charly-image:layer` "Shell Init Surface" where applicable); markdown
  integrity (no broken tables/links introduced).

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; documentation reviewed)

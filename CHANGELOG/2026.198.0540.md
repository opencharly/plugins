# 2026.198.0540 — P14: `charly box merge` skill-doc sweep (companion charly + sdk legs)

Companion docs leg for the P14 `charly box merge` whole-file move
(`opencharly/charly` branch `feat/k6-status-collector-plugin-substrate`,
expanded from K6; `opencharly/sdk` branch `feat/k6-status-fanout-op`).
`charly/merge.go` is deleted whole and relocated to
`candy/plugin-box/merge_cmd.go`, reaching `verb:oci` directly via
`InvokeProvider` (no host reentry). Two skills carried stale claims,
discovered during the R1 claim-keyed sweep on this exact topic:

- `build/skills/build/SKILL.md` — cited `charly/merge.go` as the merge
  algorithm's Source (line 389) and `mergeAfterBuild()` as the inline-merge
  function (the "Inline Merge" section). BOTH were already stale BEFORE this
  cutover: the actual go-containerregistry engine has lived in
  `candy/plugin-oci/merge.go` since the P14a cutover, and `mergeAfterBuild()`
  was replaced by `candy/plugin-build/drive.go`'s `mergeBox` at an earlier
  wave. Corrected both to the accurate current architecture.
- `build/skills/merge/SKILL.md` — cited `charly/merge.go:saveImageToDaemon`
  as the Source for the keep-on-fail diagnostic logic; `saveImageToDaemon`
  was NEVER in `charly/merge.go` (verified: it's always lived in
  `candy/plugin-oci/merge.go`). Corrected.

Every claim cross-referenced against the actual current source (`grep -n
"func saveImageToDaemon"` / `func mergeAfterBuild"` across the whole repo) —
none is a guess.

Zero behavior change — documentation only.

Assisted-by: Claude Code Anthropic Claude Sonnet 5 (documentation reviewed)

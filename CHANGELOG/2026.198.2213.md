# 2026.198.2213 — Docs-batch 6: W9 plugins leg (3 members)

Thematic docs-only batch riding the W9/K1 loader-cone wave's plugins leg
(task #56). Three members, all comment/prose-only, zero behavior change:

1. **install-plan:359 polish** — `internals/skills/install-plan/SKILL.md`
   named `renderBuilderScript` with no location, inconsistent with the
   fully-qualified form the rest of the skill set now uses since the W3
   build-engine relocation (`deploykit.RenderBuilderScript`,
   `sdk/deploykit/localpkg.go`). Polished for consistency; no claim was
   actually false, just under-specified relative to its siblings.

2. **The mutating-command cwd-anchor rule** (`internals/skills/git-workflow/SKILL.md`)
   — a stronger, WRITE-side sibling of the already-landed #48 stale-cwd
   footgun (which covered only READ-side grep/metric commands). New rule:
   a MUTATING command (writes files, or runs `git submodule update` as a
   side effect — `task cue:gen` is the canonical offender) run against a
   stale, ambient shell cwd doesn't just misreport, it MUTATES the wrong
   tree. Backed by a live incident this session: a fresh evaluator ran
   `task cue:gen` with a persisted cwd that had drifted to the main session
   worktree, and the task's own `git submodule update` chain rewound 5
   submodule checkouts + the `pkg/arch` gitlink there before the mistake
   was caught (fully restored, disclosed) — independently reconfirmed when
   a second agent, running the identical `task cue:gen` invocation in this
   same wave, found its own shell cwd similarly stale and avoided the
   incident only by anchoring an explicit `cd <worktree> &&` in the same
   compound command. Rule: every mutating task/command invocation in an
   isolated-worktree workflow carries that explicit anchor.

3. **charly candy/tailscale-up phantom file reference** — a second instance
   of the SAME false claim `2026.198.0958`'s solo fix addressed (below),
   found in `candy/tailscale-up/charly.yml`'s task-body comment (in the
   charly repo, not this one) — the R1 claim-keyed sweep's own remit ("fix
   EVERY instance, not just the file where it surfaced") caught it as part
   of this same wave; the charly-repo fix rides charly's own W9 commit
   sequence, not this PR. Recorded here as the batch's full narrative.

## Change class

Documentation-only (comment/prose edits in two `SKILL.md` files). Tier:
`documentation reviewed` — no runtime surface to gate; the non-runtime
standards (adversarial consistency review, R5 grep self-test, markdown
integrity) all pass.

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; documentation reviewed)

# 2026.198.0501

## K6 status_collector whole-file-move skill sync

Companion docs leg for the K6 cutover (`opencharly/charly` branch
`feat/k6-status-collector-plugin-substrate`, held pending this leg + the sdk
`verb:status-fanout` op leg, `opencharly/sdk#67`): `charly/status_collector.go`
and `charly/status_substrate.go` are deleted whole, their logic (the
substrate fan-out + deploy-cone enrichment) relocated to
`candy/plugin-substrate/status_flat.go`, and `charly/status_substrate_host.go`
becomes a thin generic HostBuild forward with zero status-specific logic
remaining in core. Three skills described the old file as a live core
"stays core, registry-boundary blocker" — corrected to the new two-home split:

- `core/skills/charly-status/SKILL.md` — rewrote the "Source layout" section
  (three homes → two: command:status plugin + substrate plugin; core keeps
  no status business logic).
- `core/skills/service/SKILL.md` — repointed the reap-orphans Source line.
- `internals/skills/go/SKILL.md` — repointed the file-map row.

Every claim was cross-referenced against the K6 end-state (`feat/k6-status-
collector-plugin-substrate`, not yet merged to charly `main`) — this leg's
present-tense claims become true only once the companion charly leg lands;
see the PR discussion for the landing-order resolution.

Zero behavior change — documentation only.

Assisted-by: Claude Code Anthropic Claude Sonnet 5 (documentation reviewed)

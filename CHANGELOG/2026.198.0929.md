# 2026.198.0929 — W3 skill-sync: LocalPkg stale-claim sweep

Documentation-only sync riding the W3 build-engine wave (charly
`feat/w3-build-engine-plugin` + sdk `feat/w3-buildkit-engine`): six skill files
corrected after the sdk-leg move made their "host-coupled" classification of
`local-pkg-install` / `renderLocalPkgImageInstall` false.

- `internals/skills/install-plan/SKILL.md` — the C1.4 table row + three prose
  passages reclassified: `local-pkg-install`'s render is now a PURE
  `sdk/deploykit` function (no `*Config`, no live `*Candy` graph); it still
  routes through the `step-emit` seam only for `buildEngineContext` threading
  uniformity with its genuinely host-coupled siblings (system-packages/
  builder/op), not because the render needs a host callback. "Machinery:
  `charly/localpkg.go`" corrected to `sdk/deploykit/localpkg.go`.
- `build/skills/generate/SKILL.md`, `internals/skills/generate-source/SKILL.md`,
  `build/skills/build/SKILL.md`, `internals/skills/plugin/SKILL.md`,
  `tools/skills/charly/SKILL.md` — each corrected the unqualified
  `renderLocalPkgImageInstall` mention to `deploykit.RenderLocalPkgImageInstall`
  (relocated, W3).

This is the IN-WAVE blocking sweep for claims this cutover makes false (R1/R5,
claim-keyed) — not the general docs-batch (#43-#49), which remains a separate,
still-open thematic batch for OTHER pre-existing findings.

Assisted-by: Claude Sonnet 5 (documentation reviewed)

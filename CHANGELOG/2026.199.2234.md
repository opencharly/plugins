# 2026.199.2234 — `kimi` candy owning skill

`coder/skills/kimi` is the owning skill for the new `kimi` candy (superproject
`candy/kimi`): the Moonshot Kimi Code CLI installed globally via npm from
`@moonshot-ai/kimi-code`, depending on `nodejs`, and verified by the candy's
baked checks (the `~/.npm-global/bin/kimi` binary, the unpacked scoped package
under `~/.npm-global/lib/node_modules/@moonshot-ai/kimi-code`, and
`kimi --version` printing a semantic version). The skill documents the candy
properties, the composition usage, and the sibling AI-CLI relationship
(claude-code/codex/gemini — all share the npm-global install pattern), and
names the `@github`-pin metalayer/coder-box composition as the reconcile
follow-up once the candy carries a published tag. `README.md`'s per-pod plugin
index syncs the `charly-coder` skill count 31 → 32 for the new skill.

Assisted-by: Kimi Code Moonshot Kimi K3 (documentation reviewed)

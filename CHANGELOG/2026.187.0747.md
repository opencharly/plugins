# Schema compaction docs sweep + concurrency / parallel-testing guidance

Swept every skill to the compact node grammar (the schema-compaction cutover):
collections inline in the kind value, the ordered unnamed `plan:` step list, the
`<word>: <input>` plugin-verb sugar (authored `plugin:`/`plugin_input:` in a step
is now a load error), the 11 live verbs' method+modifier fields owned by their
plugins, `env:` as a map everywhere, and the removal of the Calamares
`target:`/`module:`/`package-group:` kinds. Updated the `layer-validator` agent's
invariants for the new grammar and the per-directory CLAUDE.md signposts.

Added the concurrency / parallel-testing doctrine to `/charly-internals:agents`
("Bed-scoped parallel real-deployment testing"): the four concurrency ceilings
and their fixes — the podman store lock (`transient_store`), exclusive-resource-
token SERIAL groups (`nvidia-gpu`/`test-lock`; the arbiter fast-fails a second
same-token claim), persistent-session `run_in_background` ownership of every
>600s bed + never force-terminating a roster, and a shared-tree WALK that
tolerates a sibling build's transient `EACCES` artifacts. Made PORT
AUTO-ALLOCATION mandatory for every eval bed (`ssh.port_auto` / `port: [auto]` +
`${HOST_PORT}`), replacing the fragile manual disjoint-port guidance.

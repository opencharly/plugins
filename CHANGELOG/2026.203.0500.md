# 2026.203.0500 — Unit-6 wave, plugins docs leg: R5 sweep for S3b + prior K5-unit-6a moves

## What this cutover is

The plugins (skills) documentation leg of the Unit-6 core-minimization program's
S3b implementation unit (deploy-dispatch cluster → `candy/plugin-bundle`,
superproject `37a8ce8`, sdk `d5dc413`, base `feat/unit6-s3`). A pure
documentation-only R5 claim-keyed sweep: every skill paragraph, file-layout
table row, and cross-reference asserting the now-DELETED core deploy-dispatch
files as current architecture is rewritten against the wave tree's actual
state, verified file:line against the live source (never from memory).

## What was stale and why

S3b deleted `charly/deploy_target_external.go` (`externalDeployTarget`),
`charly/substrate_lifecycle_grpc.go` (`grpcSubstrateLifecycle`),
`charly/deploy_preresolve.go` (`wireDeployPreresolver`), and
`charly/deploy_substrate_lifecycle.go` (the `substrateLifecycle` interface),
replacing them with a thin, data-only core proxy (`pluginDeployTarget` in
`charly/unified_targets.go` + `charly/deploy_target_dispatch.go` +
`charly/arbiter_bracket.go`) that dispatches every deploy-lifecycle method
through `candy/plugin-bundle`'s `Invoke(sdk.OpDeployDispatch)`, which in turn
reaches each concrete substrate provider via its own `sdk.Executor.InvokeProvider`
(S1). The sweep additionally uncovered — via ground-truth code reads, not
assumption — that an EARLIER cutover (FINAL/K5 unit 6a, M4b) had already
deleted the vm substrate's host-side `lifecyclePrepareHook` data-seam and the
ENTIRE android substrate's host-side preresolve files
(`charly/android_spec.go`, `android_deploy_cmd.go`,
`android_deploy_preresolve.go`) plus the k8s host-side preresolver
(`charly/k8s_deploy_preresolve.go`) — all relocated wholesale into their
respective out-of-process plugins (`candy/plugin-deploy-vm/lifecycle.go`,
`candy/plugin-adb/preresolve.go`, `candy/plugin-kube/preresolve.go`) reaching
the host only via the generic `"deploy-entity-resolve"` HostBuild seam. Several
skills still described the pre-6a two-part (host-hook-then-plugin) shape; those
are corrected in the same sweep since they carry the identical false-claim
class (R1 "documentation divergence is an incident" — claim-keyed, not
limited to the S3b-specific identifiers named in the brief).

Also fixed: the `charly/plugin_executor_reverse.go` S4 collapse of
`executeExternalPluginStep`/`executeExternalStep` into the single shared
`invokeExternalStep` dispatch; `charly/k8s_plugin.go`'s `invokeKubePlugin` →
`invokeKubePluginWithBroker` rename and the `k3s-post-provision` method
rename (from the retired `merge-kubeconfig` Op.Kube dispatch shape); and the
`sdk/vmshared/cloud_init_render.go` D15 pacman-family packages delivery
branch (a `runcmd`-prepended `pacman -S --needed` instead of the `packages:`
cloud-config key, to avoid the openssh-reinstall SSH-reset race) that
`cloud-init-renderer`'s SKILL.md had not yet documented.

## Docs/lessons batch addendum: pr-validator.md model-name clarification

Riding this open cutover per the Cutover Sizing Law (a small, non-blocking,
docs-only fix batched onto the nearest open docs PR rather than paying its
own solo landing ceremony): `internals/agents/pr-validator.md`'s "Attribute
the comment" instruction (the PR-comment footer, ~line 591) named
`<Harness>`/`<Full Model Name>` only as a template token without stating they
are placeholders to fill from runtime-verified identity — validators had
typed plausible-but-wrong model names into PR-comment footers (while the
separate commit-trailer verification instruction, item 3, was followed
correctly). The instruction now explicitly says `<Harness>` and
`<Full Model Name>` are format placeholders, points at the SAME
runtime-verification step item 3 already requires for the commit trailer,
and forbids a typed/recalled/guessed name. No behavior change to the
commit-trailer path, which was already correct.

## Files changed (16)

`internals/skills/install-plan/SKILL.md` (primary target — the deploy-target
architecture section, file-layout table, Op-selector bullets, cross-references),
`internals/skills/go/SKILL.md`, `internals/skills/plugin/SKILL.md`,
`internals/skills/vm-deploy-target/SKILL.md` (the largest secondary sweep —
the vm preresolve-collapse finding), `internals/skills/local-infra/SKILL.md`,
`internals/skills/generate-source/SKILL.md`, `internals/skills/cloud-init-renderer/SKILL.md`,
`build/skills/build/SKILL.md`, `build/skills/generate/SKILL.md`,
`core/skills/deploy/SKILL.md`, `local/skills/local-deploy/SKILL.md`,
`kubernetes/skills/kubernetes/SKILL.md`, `kubernetes/skills/check-k8s/SKILL.md`,
`check/skills/android/SKILL.md`, `check/skills/adb/SKILL.md`, and (the
docs/lessons batch addendum above, a separate theme) `internals/agents/pr-validator.md`.

Every rewritten claim was verified against the wave tree (`/home/atrawog/Atrapub/o/charly-wt-fss3b`)
file:line before landing — deleted-file claims confirmed by absence on disk,
replacement mechanisms confirmed by reading the actual current source
(`charly/unified_targets.go`, `charly/deploy_target_dispatch.go`,
`charly/arbiter_bracket.go`, `candy/plugin-bundle/deploy_target.go`,
`candy/plugin-deploy-vm/lifecycle.go`, `candy/plugin-adb/preresolve.go`,
`candy/plugin-kube/preresolve.go`, `charly/plugin_executor_reverse.go`,
`charly/k8s_plugin.go`, `sdk/vmshared/cloud_init_render.go`).

## R5 grep self-test

Every one of the following now returns ONLY historical "was X, DELETED, now Y"
migration-rationale prose across `plugins/**/SKILL.md` (verified — see the
sweep transcript): `deploy_target_external.go`, `substrate_lifecycle_grpc.go`,
`deploy_preresolve.go`, `deploy_substrate_lifecycle.go`, `externalDeployTarget`,
`grpcSubstrateLifecycle`, `registerPluginSubstrateLifecycle`,
`executeExternalPluginStep`, `executeExternalStep`, `invokeStepExecute`,
`externalStepVenueName`, `android_deploy_preresolve.go`,
`android_deploy_cmd.go`, `android_spec.go`, `k8s_deploy_preresolve.go`. No
live (non-historical) hit remains.

## Battery

Documentation-only change class — no code/config touched, zero behavior
change. Non-runtime standards only: markdown integrity (balanced backticks,
verified table-cell literal pipes correctly escaped `\|`), adversarial
consistency pass (every claim cross-checked against the actual wave-tree
source, not memory or the prior skill text), and the R5 grep self-test above.

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; documentation reviewed)

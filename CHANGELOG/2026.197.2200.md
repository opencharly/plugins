# 2026.197.2200 — Docs sweep: kit_aliases.go/buildkit_aliases.go stale claims (K3 rider)

Documentation-only, one file touched in `plugins`, riding the `charly` repo's
K3 core-minimization cutover (the READY-NOW alias-dissolution opener): the
`charly` change deletes `kit_aliases.go`, `buildkit_aliases.go`, every
`deploykit_*_aliases.go`, `ssh_config_aliases.go`, and `build_stage_atomic.go`
from `charly/`, repointing every call site to `spec.X`/`deploykit.X`/
`kit.X`/`buildkit.X` directly. `internals/skills/go/SKILL.md` named those
files, still present, in its alias-surface bullet and the `checkspec.go` /
`sdk/kit/checkvars.go` / `checkrun.go` table rows — an R1 documentation
divergence surfaced by the same cutover that caused it, swept here rather
than left for a later batch (CLAUDE.md R1: "documentation divergence is an
incident," claim-keyed, fixed in the same cutover).

## Change

`internals/skills/go/SKILL.md`:

- The alias-surface bullet now names only `vmshared_aliases.go` as the
  currently-remaining transitional `charly/*_aliases.go` file, and states
  (as a stable fact, not a live count) that `kit_aliases.go`,
  `buildkit_aliases.go`, every `deploykit_*_aliases.go`,
  `ssh_config_aliases.go`, and `build_stage_atomic.go` were dissolved in K3.
- The `checkspec.go` row's `${VAR}`-expansion aside, the `sdk/kit/checkvars.go`
  row, and the `checkrun.go` row each dropped their "residual
  `kit_aliases.go` binding" hedge — package main now references `kit.X`
  directly with no remaining alias for these symbols.

## Change class + tier

Documentation-only — one file (`internals/skills/go/SKILL.md`) + this
CHANGELOG entry. Zero behavior change, no Go / YAML-schema / hook / workflow
code touched. Gate: the non-runtime standards (adversarial consistency
review; markdown integrity — table row/pipe balance checked per touched
line; R5 grep self-test — `git grep -n 'kit_aliases\.go\|buildkit_aliases\.go'`
in this repo now returns only historical `CHANGELOG/` mentions and this
entry). Tier: `documentation reviewed`.

*Assisted-by: Claude Sonnet 5 (documentation reviewed)*

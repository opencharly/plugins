# 2026.205.0715 — Generic agent-policy and attribution contract

The agent-policy documentation now treats `AGENTS.md` as the
harness-neutral rulebook and `CLAUDE.md` as a harness adapter with equivalent
overall policy. Skill-maintenance and agent guidance use those ownership terms
without assigning the generic rulebook to one product.

The PR-validator brief now uses the canonical attribution shape everywhere:

`Assisted-by: <Harness> (<Provider Full Model Name>; <confidence>)`

The Git workflow example uses the same provider-qualified placeholder instead
of embedding one harness identity. These documentation-only changes were
checked for attribution-shape consistency, current cross-references, Markdown
integrity, and stale policy wording.

Assisted-by: Codex (OpenAI GPT-5.6 Sol; documentation reviewed)

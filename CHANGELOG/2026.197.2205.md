# 2026.197.2205 — docs sync for the check-bed session move (K5-U1)

Documentation-only follow-through for the K5-U1 cutover (the check-bed
SESSION — locks/lease/env/BedDescriptor — moves from `charly/
host_build_check_bed.go` into the compiled-in `candy/plugin-check` plugin,
`bed_session.go`). Two skills carried stale claims about this seam; a third,
unrelated staleness surfaced by the same investigation is fixed alongside it
(R1: documentation divergence is an incident, fixed on discovery).

## What changed

- `internals/skills/go/SKILL.md`: rewrote the `host_build_check_bed.go` /
  `check_bed_run.go` table rows to describe the narrowed 9-op shared-helper
  seam (the session itself is gone from this file). Also corrected two
  ALREADY-STALE rows discovered in the same pass: `check_runner_cmd.go` and
  `host_build_check_config.go` are BOTH already deleted from `charly/` core
  (a prior K5-U2/3 cutover removed them, per `candy/plugin-check/
  checkproject.go`'s own header) but the table still described them as
  live files — struck through and corrected to point at their actual
  successors (`host_build_pod_disposable.go`'s thin seam;
  `checkproject.go`'s `resolveCheckProjection`).
- `check/skills/check/SKILL.md`: corrected the "check-bed setup op ... drives
  the host `check-bed` session seam" claim — setup is now plugin-side; the
  host is reached only for the stateless `repo-override-pair` identity
  query, and the plugin does its own `os.Setenv`/restore (safe: a
  compiled-in plugin shares the host's OS process).

## Change class + tier

Documentation-only (`*.md`, zero behavior change) — no bed run, no build.
Gated by the non-runtime standards: adversarial consistency review against
the actual superproject cutover this doc sync accompanies, cross-reference
validation (verified `host_build_pod_disposable.go` and `checkproject.go`'s
`podDisposable` really exist and do what the corrected text claims), and
markdown integrity.

*Assisted-by: Claude Code Claude Sonnet 5 (documentation reviewed)*

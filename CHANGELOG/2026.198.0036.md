# 2026.198.0036

## Status-subsystem skill sync (K5)

- `charly-status/SKILL.md`: replaced the stale `SubstrateCollector`/
  `init()`-registry description (deleted by the merged K5 status-collector
  cutovers, #135/#136 in the `charly` repo) with the current generic
  word-dispatched `OpStatusCollect` architecture; updated the Source layout
  to name the five substrate collectors' real files in
  `candy/plugin-substrate` (`status_vm.go`/`status_k8s.go`/
  `status_android_collect.go`/`status_pod.go`/`status_local.go`), the
  declared-nested-tree pre-resolution's new home
  (`candy/plugin-status/nested_tree.go`), and the externalized
  `charly reap-orphans` command (`candy/plugin-substrate/command_reap_orphans.go`).
- `service/SKILL.md`: corrected an actively wrong flag in the reap-orphans
  section — documented `charly bundle del <name> --force`, a flag that does
  not exist on `bundle del` (the real flag is `--assume-yes`, per
  `deployDelArgv`'s own doc comment and its regression test, which notes
  this exact `--force`/`--yes` drift previously aborted teardown silently).

Every claim cross-referenced against the current `charly` checkout
(file existence via `ls`, the flag via `grep`) — none is a guess.

Assisted-by: Claude Sonnet 5 (documentation reviewed)

# 2026.203.1244 — dead-code radical-removal batch (twin-or-regression docs leg)

Docs fix surfaced by the batch's twin-or-regression procedure on the 4
previously-flagged only-test-called validators/renderers (companion PR:
`opencharly/charly#179`).

## Fixed (R1 — documentation divergence)

- **`internals/skills/go/SKILL.md`** claimed `validateVocabularyCollections`
  (`charly/validate.go`) was "also used by `charly box validate` for project
  files" — false on two counts: (1) that production call site was already
  deliberately cut in charly's `c9befd83` (the legacy root-shape collection
  format it validated became unreachable, hard-rejected at load before ever
  reaching validation); (2) `validateVocabularyCollections` and its sibling
  `validateEntityCUE` are themselves deleted as dead code in the companion
  charly PR — RDD-verified live that the modern per-kind load-time plugin
  gate (`plugin kind:<X>: plugin_input fails #<X>Input`) is the real
  production entity-schema enforcement today (tested live against real
  `builder:`/`agent:` entities carrying an unknown field, both correctly
  rejected at load). Corrected 3 locations: the embedded-defaults paragraph,
  the `validate.go` file-map table row, and the schema-change guards list.

## Change class

Documentation-only — zero behavior change. Confidence tier: `documentation
reviewed`.

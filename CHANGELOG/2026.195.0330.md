# 2026.195.0330 — R44 docs — deploy-executor path corrections + check-box reap paragraph → Option A

Rides the R44 exit-127 check-box store-contention fix (task #44).

- **Path corrections (mechanical, R5 claim-keyed):** `charly/deploy_executor*.go` → `sdk/kit/deploy_executor*.go`
  in five skills (agents, install-plan, local-infra, go, vm-deploy-target) — the whole deploy-executor family
  (`deploy_executor.go` / `_nested.go` / `_ssh.go`) moved to `sdk/kit`. Paths only, no content rewrites.
- **Check-box reap paragraph (agents skill):** rewritten from the R44-DELETED per-step reap
  (`reapDisposableProbe` naming each `charly-probe-<pid>-<seq>` container and force-reaping it) to the Option-A
  model — `charly check box` now runs its probes in ONE persistent container
  (`checkBoxContainerChain`: `podman run -d … sleep infinity` once + `podman exec` per step + one `rm -f`),
  O(N)→O(1) container setups, and a residual setup failure is classified INFRA (exit-1, never checks-failed).

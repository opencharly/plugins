# M4b doctrine follow-up: vm lifecycle DONE in the Remaining program

Immediate follow-up to M4b (R2): the doctrine's Remaining program listed the vm substrate lifecycle as
IN FLIGHT — M4b externalized it (candy/plugin-deploy-vm forwards to charly __vm-lifecycle over the cli
seam), so the plugin skill's Remaining program now marks deploy:vm lifecycle DONE (both pod + vm venue
lifecycles externalized), and the intro reflects both substrate lifecycles as externalized. Pairs with
the CLAUDE.md Key Rule update (main repo).

# 2026.193.0524 — R5 skill sweep: vm source-map refs point at their post-P10 homes

The P10 cutover (VM CLI family → `candy/plugin-vm`) moved 11 core CLI files into the compiled-in
`command:vm` plugin, but 15 skill lines still named the OLD `charly/` paths — a failing R5 grep
self-test (`git grep 'charly/vm.go'` returned live skill hits, not just CHANGELOG). All fixed here
so every source-map reference names the file's actual post-P10 home:

- **Fully moved to the plugin** → `candy/plugin-vm/…`: `vm.go` (lifecycle), `machine.go` (podman
  machine), `vm_create_spec.go` (`charly vm create` CLI wiring), `vm_gpu_cmd.go` (gpu status/recover/
  plan), `vm_libvirt.go` / `vm_qemu.go` (backends), `ensureBootAutostartPrereqs`, and the
  `ResolveOvmfForSpec` call site (`vm_create_orchestrate.go`).
- **Core-retained seam** → `charly/vm_backend_lifecycle.go`: `resolveVmBackend`,
  `startLibvirtUserSession`, `generateSSHKeypair`, the `qemu:///session` default (the old
  `charly/vm.go:22` line-number refs dropped; one was mis-homed to `vm_cloud_image.go`).
- **Stays core (build engine)**: `charly/vm_build.go` + `charly/vm_cloud_image.go` refs kept (the
  VM-disk build engine stays core behind `HostBuild("vm-build")`); the co-mentioned `http_fetch.go`
  re-homed to its actual P4 home `sdk/kit/http_fetch.go` (`FetchQcow2`).

Files: `internals/skills/{go,vm-deploy-target,vm-spec,libvirt-renderer,ovmf}/SKILL.md`,
`vm/skills/{vm,arch}/SKILL.md`, `check/skills/check/SKILL.md`,
`infrastructure/skills/virtualization/SKILL.md`. Every symbol home verified on the working tree
before editing (the plugin carries the moved files; `vm_backend_lifecycle.go` carries the retained
seams). R5 grep self-test now clean. Pairs with the superproject P10 landing (the code cutover +
the 21-symbol dead-code sweep) whose gitlink bump carries this commit.

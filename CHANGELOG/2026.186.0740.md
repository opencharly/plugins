# skills: vm venue lifecycle implemented in the plugin (clean externalization) — R5 claim sweep

Claim-keyed R5 sweep for the clean vm-substrate externalization: the venue lifecycle is now IMPLEMENTED
in candy/plugin-deploy-vm over generic seams (sdk/kit for the ssh-config stanza + guest waits + charly
delivery; HostBuild("cli") for `charly vm …`; the reverse channel for nested pods), consuming only the
host-resolved spec.LifecyclePrepareInput a lifecyclePrepareHook (charly/vm_lifecycle_preresolve.go)
ships — the same DATA-seam shape as the in-core k8s/android preresolvers. NO vm lifecycle logic remains
in core. Repointed every stale reference to the DELETED vm_deploy_lifecycle.go / vmSubstrateLifecycle
struct / __vm-lifecycle command / deployNestedPodsInGuest / charly_install.go / ssh_managed_config.go
across vm-deploy-target, go, install-plan, plugin, cloud-init-renderer, vm-spec, local-infra, libvirt,
core/deploy, and vm/cachyos skills. git-grep self-test: zero live-doc hits for any deleted identifier.

# 2026.193.2140 — codify 3 agent-orchestration insights: slot budget, handoff-via-worktree, tag-verify (#36)

Documentation-only cutover folding this session's PROVEN, generalizable agent-orchestration
insights into the committed skills (the operator's "constantly update skills with generalizable
insights" directive). The paired superproject pointer bump rides the same logical cutover.

**`internals/skills/agents/SKILL.md`:**

- New subsection **"Slot budget — a persistent slot is a concurrent CUTOVER, not an agent"** under the
  default execution model: a persistent teammate slot ≈ ONE concurrent independent cutover (NOT an
  agent headcount); sequential sub-phases WITHIN a cutover compact-and-continue + delegate mechanical
  bulk to TRANSIENT sub-agents (which free their slot on return), never a new persistent teammate;
  validators + design-reserves are transient/recyclable (stop when the role completes); on slot
  exhaustion STOP STALE AGENTS, never downgrade to headless/in-process (the panes are the operator's
  live oversight). Links the existing "Agent lifecycle hygiene" rather than restating it (R3).
- The **"Delegation IS fresh context"** section gains: a handoff PRESERVES in-flight WIP via the
  non-destructive git WORKTREE + a handoff package, NEVER a "checkpoint commit" — un-R10'd non-docs
  code cannot be committed at any honest tier (`syntax check only` pairs with "do NOT commit"; a
  runtime tier needs a live R10), so the gate blocks it and the worktree is the durable carrier.

**`internals/skills/git-workflow/SKILL.md` + `internals/agents/pr-validator.md`:**

- The "every repo tagged at merge" invariant + the validator's Tag step now state: a skipped tag is
  a DEFECT, not an exemption; the pre-unification `plugins`/`pkg` tag-exempt rule is RETIRED (a
  recalled prior asserting it is STALE — it once shipped a `plugins` merge un-tagged); the
  orchestrator VERIFIES the tag after each merge (`git ls-remote --tags`) and BACKFILLS it add-only
  on the merged HEAD if the evaluator skipped it (tags are immutable — a backfill only adds one).

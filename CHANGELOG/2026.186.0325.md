# M4b skill sweep: vm deploy lifecycle externalized to candy/plugin-deploy-vm

Claim-keyed R5 sweep for M4b (vm substrate lifecycle out of core — completing Cutover 3). The
compiled-in registerSubstrateLifecycle("vm",…) is deleted: candy/plugin-deploy-vm now declares
Lifecycle:true and forwards each host-side venue-lifecycle Op to the hidden `charly __vm-lifecycle
<op> <name>` over the generic "cli" seam (the vm analog of pod's HostBuild("overlay") — the
host-coupled vmSubstrateLifecycle methods stay core because they NEED core: LoadUnified→spec.Vm,
libvirt boot, ssh-config, EnsureCharlyInGuest, VmDeployState). Repointed the "registered via
registerSubstrateLifecycle('vm',…)" / "the one external substrate that owns a real venue lifecycle"
claims across vm-deploy-target, go, and install-plan (both pod + vm now own externalized venue
lifecycles). R10: check-charly-vm + check-k3s-vm both PASS (steps=6).

# 2026.193.1942 — hooks doctrine: CHANGELOG required at EVERY legal tier (close the #35 gap, plugins half)

Documentation-only cutover — the plugins half of #34 ("same changelog rules for plugins"). The
paired mechanical change (the `pre-commit-gate.sh` widening + its `gate_test.py` cases) and the
superproject `CLAUDE.md` hooks-doctrine mandate land in the superproject PR; this plugins producer
merges first (B2 order).

**`internals/skills/agents/SKILL.md`:**

- The hooks-doctrine `pre-commit-gate.sh` block-list entry that read "a RUNTIME-tier commit
  (`fully tested and validated` / `analysed on a live system`) that stages no
  `CHANGELOG/<YYYY.DDD.HHMM>.md` entry" now reads "a commit at ANY legal tier
  (`fully tested and validated` / `analysed on a live system` / `documentation reviewed`) …". The
  gate historically fired the CHANGELOG check only for the two runtime tiers, so a
  `documentation reviewed` doc/skill edit in a CHANGELOG-tracking repo could skip its history entry
  (the gap PR #35 hit — caught by T-P14b's convention, not the gate). The pure-submodule-pointer-bump
  exemption is unchanged (a pure pointer bump's narrative lives in the submodule).

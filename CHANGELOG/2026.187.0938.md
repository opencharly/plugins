# migrate skill: #Migration schema now lives in the plugin

R5 sweep for the moved `migration.cue`. `/charly-build:migrate` now names the
`#Migration` schema at its new home — `candy/plugin-migrate/schema/migration.cue`
(a plugin-only validation schema, per the kernel/plugin boundary law, concatenated
at compile with the SDK's `version.cue` for `#CanonCalVer`) — instead of the old
`sdk/schema/migration.cue`.

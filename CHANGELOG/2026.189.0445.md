# 2026.189.0445 — pr-validator Phase-1 checklist: enforce EVERY CLAUDE.md rule

Follow-up to the anti-cheat commit (2026.189.0438, which added the top-line total-
enforcement mandate + the forbidden-framing item): rewrites the Phase-1 checklist
from 8 coarse items into a comprehensive, change-class-scoped 0–18 that maps ONE
explicit check to EVERY CLAUDE.md rule and mandate — a validator that samples the
rules is not a gate.

The 0–18 items: security screen (0); description + change-class R7/R10 gate incl.
the R10 fraud clauses + attribution ceiling (1–3); each ground-truth rule R0–R10
INDIVIDUALLY, incl. the zero-warnings gate (4–11); RDD / ADE (description+plan+≥1
deterministic check per candy) / SDD (`task cue:gen` no-op, no generated-artifact
drift) (12); the concurrency mandate + the forbidden-framing anti-cheat (13); Hard
Cutover one-atomic-phase + plan-as-contract (14); the kernel/plugin boundary law —
no concrete-kind leak into core/sdk (15); disposable-only autonomy (16); clean
architecture + the Go gates gofmt/golangci-lint-0/vet/test + repo invariants
(mode purity, globally-unique node names, YAML↔Go symmetry) (17); CHANGELOG (18).
The output-format block gains the matching 0–18 lines, each markable [N/A] with a
one-line reason where the change class excludes it. Closing line: a rule not
positively confirmed from the diff + the validator's own re-run is unverified =
FAIL until the author supplies the proof.

*Assisted-by: Claude (documentation reviewed)*

# 2026.198.0928 — W3 plugins docs batch 5: guardrails, footguns, and pending-claim sweep

Consolidated documentation-only batch riding the W3 build-engine wave's plugins
leg (operator standing order: one plugins PR per wave — every pending docs item
rides it, no solo docs PRs). Folds in two already-authored branches plus the
standing docs-batch members tracked since the wave-1 landing:

**Folded in (pre-existing work, defects fixed on fold-in):**

- T-w3-build's own wave sweep — the LocalPkg stale host-coupled claims across
  6 skills (install-plan, plugin, build, generate, generate-source, charly),
  plus the install-plan C1.4 row.
- The k6-status-collector-skill-sync branch (plugins#84, 3 commits): the
  charly-status/service/go skill sync for the K6 status_collector whole-file
  move, and the P14 `charly box merge` skill-doc sweep. Two validation-round
  defects fixed on fold-in: both new CHANGELOG entries had their title as a
  separate H2 instead of the shipped single-line `# <CalVer> — <title>` H1;
  and `build/skills/merge/SKILL.md` still asserted `mergeAfterBuild`
  (`charly/build.go:178-186`) as the live merge-failure handler after the
  branch's own sweep — verified false (that range is `pruneAfterBuild`,
  image-tag retention, unrelated) and corrected to the actual `mergeBox`
  (`candy/plugin-build/drive.go`).
- A pre-existing H1/filename mismatch in `CHANGELOG/2026.196.1135.md`
  (`# 2026.196.9999` → `# 2026.196.1135`, matching its filename).

**New batch members:**

- **Install-plan relocation stale-claim fix (R1 claim-keyed sweep).** The
  "File layout" table's `charly/install_plan.go` row claimed the file still
  defines `InstallPlan`/`DeployTarget`/`InstallStep`/`EmitOpts`/`ReverseOp`/
  `GateEnabled` natively — stale since the SDK envelope relocation (already
  completed) moved every one of them to `sdk/spec` (+ `GateEnabled` to
  `sdk/deploykit/plan.go`). Verified against the current tree and corrected;
  the file itself now holds only host-side carrier interfaces.
- **Three landing-mechanics footguns** (git-workflow): merge-time CalVer must
  come from the live UTC clock, never a commit's recorded date (`git show
  --date=format:`/`git log --format=%cd` read the commit's own TZ offset and
  can mis-stamp by hours); a metric/grep verification command must be
  anchored to an explicit repo root, never an ambient cwd; `git stash`/`pop`
  can silently un-stage a `git rm`.
- **The universal PR-gate** (agents + git-workflow): before opening,
  updating, or merging any pull request, run the aggregate audit (`gh pr
  list` across every touched repo + `git worktree list` + the live
  teammate/agent roster) unconditionally, as a standing preflight — never
  situational advice reached for only once something looks off.
- **Enforcement guardrail for "host-coupled is never a permanence reason"**
  (agents + plugin): a mover reports where each externalized piece landed; a
  validator rejects a remainder whose only justification is a stays-core
  header, demanding call-chain evidence; the orchestrator audits stays-claims
  against the boundary law with that evidence. Precedent cited:
  `charly/host_build_deploy_add.go`'s header still claims its deploy kernel
  "STAYS CORE" on a "cannot cross the process boundary" basis — overruled per
  operator direction as boundary-law-violating K-wave residue.
- **Two pr-validator lessons** (`internals/agents/pr-validator.md`): a
  producer-repo symbol REMOVAL claimed "dead" must check liveness against the
  CONSUMER's real `main`, never a sibling unmerged branch (the sdk#66
  over-removal incident: 4 wire types removed as dead were live on charly
  main); and concurrent validators must isolate `GOLANGCI_LINT_CACHE` per
  worktree (a shared default cache cross-contaminates findings between
  simultaneous `golangci-lint run` invocations).

**Investigated, no fix needed:** the R1 claim-keyed sweep for "an sdk-only
candy cannot satisfy `deploykit.CandyModel` from a serialized model" (once
true, in an earlier P8b-era draft of `/charly-internals:plugin`) — verified
this exact claim no longer exists anywhere in the current tree (`git grep -i
candymodel` zero-hit outside code); the #67 render-DRIVE move doc-sweep
already rewrote the paragraph that carried it. No further action.

Zero behavior change — documentation only.

Assisted-by: Claude Code Anthropic Claude Sonnet 5 (documentation reviewed)

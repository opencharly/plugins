# 2026.203.1426 — architecture-docs divergence batch: GPU/env-injection + MergeDeployShell/renderOpCommand/validatePluginCandy/ValidateXMLEgress skill sweep

Docs-only thematic batch closing the divergence cluster flagged by the
`dead-code-radical-removal batch` (superproject `CHANGELOG/2026.203.1303.md`,
"Remaining scope NOT fixed this round"): several `plugins/` `SKILL.md` files
described now-superseded charly-core architecture as current, because their
underlying Go call sites relocated into plugin candies during today's
core-minimization waves without a matching docs sweep.

## What was stale, and what's actually true now

Every claim below was verified against the LIVE `charly`/`candy/plugin-*`
source on `plugins` origin/main tip `474e530` (not assumed from any prior
skill wording or memory) before being rewritten.

| Stale claim | Current reality |
|---|---|
| `appendAutoDetectedEnv()` / `appendGroupsForAMDGPU` (the DRINODE/DRI_NODE/HSA_OVERRIDE_GFX_VERSION/keep-groups injection shared by `charly config`/`start`/`shell`) live in `charly/devices.go` | Both functions were relocated WHOLESALE to `candy/plugin-deploy-pod/config_setup_helpers.go`; the charly-core copies were deleted as unreached (test-only) residue in the 2026-07-22 dead-code-radical-removal batch. The three commands still share ONE function — reached via that candy's own `config_setup.go` (`charly config`), `resolve.go` (`charly start`/`stop`), and `resolve_f12.go` (`charly shell`/`cmd`/`logs`) — so the "one function, one env set" property is unchanged, only its file moved. `charly/devices.go` itself now keeps only `appendEnvUnique`, `LogDetectedDevices`, the embedded detection data tables, and `AutoDetectFlags`. |
| `memlockUnlimited` lives in `charly/devices.go` | Deleted from core as unreached residue; duplicated today in `candy/plugin-doctor/command.go` and `candy/plugin-vm/vm_util_copies.go`. |
| `charly/egress.go` serves four shim functions including `ValidateXMLEgress` | The core-side `ValidateXMLEgress` wrapper was deleted (zero production callers); `xml`-mode validation is still fully served by `verb:egress`'s `OpValidate` — its one real caller, `candy/plugin-vm`, now Invokes `verb:egress` directly through its own `ValidateXMLEgress` copy (`vm_egress_shim.go`). `charly/egress.go` keeps three shims (`ValidateEgress`/`ValidateEgressValue`/`validateTextEgress`). |
| `charly box validate`'s plugin-candy check is `validatePluginCandy` in core | Deleted as dead code (its call site moved during an earlier cutover, `c9befd83`); the live check is `candy/plugin-box/validate_rules.go`'s `IsPlugin` block, an explicit documented 1:1 port with the same three checks. |
| The `op` InstallStep's deploy leg renders via `resolveProvisionScript` / `renderOpCommand` | `renderOpCommand` was a test-only wrapper with zero production callers, deleted in the same batch; the live mechanism is `resolveProvisionScript` alone (`charly/checkrun_act.go`, `charly/plugin_executor_reverse.go`). |
| `MergeDeployShell` merges a charly.yml `shell:` deploy-scope overlay onto `LabelShell` | `MergeDeployShell` (+ `replaceShellEntryByID`, `shellOverlayToEntry`) had zero real callers and was deleted in the same batch. A deploy-scope `shell:` block still parses (`DeployShellOverlay`) but currently has no live merge consumer — the label's Deploy section is not populated. |

## Files corrected

- `core/skills/charly-doctor/SKILL.md`
- `core/skills/shell/SKILL.md`
- `distros/skills/nvidia-layer/SKILL.md`
- `distros/skills/rocm/SKILL.md`
- `internals/skills/capabilities/SKILL.md`
- `internals/skills/egress/SKILL.md`
- `internals/skills/go/SKILL.md`
- `internals/skills/install-plan/SKILL.md`
- `internals/skills/plugin/SKILL.md`

`core/skills/charly-config/SKILL.md` was in the originally-flagged cluster but
carried no explicit `charly/devices.go`-location claim to correct — only bare
function-name mentions, which remain accurate — so it was left untouched (R3:
no edit for what isn't wrong). `distros/skills/nvidia/SKILL.md` and
`selkies/skills/selkies/SKILL.md` were checked (the latter surfaced by the
wider claim-keyed grep sweep below) and likewise carried no location-specific
false claim.

## Sweep method (claim-keyed, R1)

Beyond the originally-flagged cluster, grepped the full `plugins` tree for
every identifier named in the dead-code-radical-removal batch's "Remaining
scope" note plus the batch's other deletions (`HasTailscaleSidecar`,
`extractQuadletImageLine`, `installHint`, `distroFamily`, `RootfulEngine`,
`CandyNames`, `ScanAllCandy`, `bareRefs`, `resolveSecretValue`,
`deployNodeSharesGPU`, `SavePortOverride`, `candySourceDirs`,
`newHostVerbResolver`, `ImageRuntime`, `ResolveBoxEngineFromDir`,
`AllVerdicts`, `validateEntityCUE`, `validateVocabularyCollections`) across
every `SKILL.md` in the repo. No further false claims surfaced — the two
already-fixed rows above (`validateVocabularyCollections`/`validateEntityCUE`
in `internals/skills/go/SKILL.md`, `ValidateLibvirtSnippet` in
`internals/skills/libvirt-renderer/SKILL.md`) were corrected by earlier PRs on
this same `main` tip (`76d06f2`, `026e264`) and are confirmed still accurate
here — not re-touched.

## Proof (Documentation-only change class)

- Every rewritten claim anchored against a fresh `grep`/`Read` of the actual
  `charly`/`candy/plugin-*` source on this branch's base commit — never
  assumed from prior skill wording (see the table above).
- R5 grep self-test: `grep -rn` for every stale claim class (function name +
  "in `charly/devices.go`" / "in `charly/egress.go`" location assertions)
  across `plugins/` returns clean outside this entry and the historical
  CHANGELOG context.
- Markdown integrity: table row column counts unchanged before/after every
  table-row edit (`git diff` column-count check); no frontmatter touched.
- Cross-reference check: no other skill references the corrected sections in
  a way that assumed the stale location.
- No `charly` build/beds run — Documentation-only change class (`*.md` only,
  zero behavior change).

## Attribution

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; documentation reviewed)

# 2026.191.1540 — go skill source map: the check result model lives in sdk/kit

Claim-keyed sweep (R1/R5) riding P5-unit-2 (sdk `v0.2026191.1732`, which moves the check
result model into `sdk/kit`).

The `/charly-internals:go` skill's `checkrun.go` source-map row said `CheckStatus`/`CheckResult`
are types defined in `checkrun.go` and that its "Output formatters: text, JSON, TAP" live there.
Both are now stale: the result model — `kit.Status` / `kit.CheckResult` / `kit.StepResult` and the
`kit.FormatStepResults{Text,JSON,TAP,JUnit}` reporters — lives in `sdk/kit`; `checkrun.go`,
`description_run.go`, and `kit_aliases.go` carry only the package-main aliases.

The row now names the kit home and the alias surface, so a reader looking for the result types
finds them where they actually are.

*Assisted-by: Claude (documentation reviewed)*

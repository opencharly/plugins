# M4a skill sweep: pod deploy lifecycle externalized to candy/plugin-deploy-pod

Claim-keyed R5 sweep for M4a (pod substrate lifecycle out of charly core): the compiled-in
podSubstrateLifecycle (charly/pod_deploy_lifecycle.go) is deleted — pod's venue lifecycle is now
served by the external candy/plugin-deploy-pod over the wire (PrepareVenue via HostBuild("overlay"),
Start/Stop/Status/Logs/Shell/Rebuild/PostTeardown via HostBuild("cli")), while the overlay BUILD
engine stays core in charly/build_overlay.go (PodDeployTarget/OCITarget, reached via the F10
"overlay" host-builder). Repointed every podSubstrateLifecycle / pod_deploy_lifecycle.go claim in the
install-plan + vm-deploy-target skills.

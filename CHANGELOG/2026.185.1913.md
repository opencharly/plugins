# Repoint the plugin surface to github.com/opencharly/sdk

25 SKILL.md files swept to the sdk module paths (sdk, sdk/kit, sdk/proto,
sdk/spec, sdk/schema, sdk/vmshared, the public sdk/schemaconcat) after the
plugin surface was extracted from the charly module into the standalone
github.com/opencharly/sdk (submodule sdk/). git-workflow gains the sdk-first
landing order + the v0.<YYYYDDD>.<HHMM> Go-module tag scheme; migrate/go skills
get the cross-repo schema-bump flow (sdk/schema/version.cue → sdk cue:gen + tag →
superproject submodule bump + charly/migrations.cue entry). Stale libvirt
file-location claims corrected to sdk/vmshared + candy/plugin-vm.

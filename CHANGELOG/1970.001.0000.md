# 1970.001.0000 — sync claude-code skill claims with the native-installer cutover

- Sync the claude-code skill with the candy's official native installer:
  the binary is relocated to the system path `/usr/local/bin/claude`, the
  nodejs dependency is gone, and the npm-global install claims are replaced.
- Sweep the sibling skills carrying the same falsified claim — nodejs,
  kimi, forgecode, hermes, and openclaw-desktop — so every reference to the
  claude CLI points at the native install location while codex and gemini
  stay on the npm-global pattern.

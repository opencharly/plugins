# 1970.001.0000 — OpenClaw disposable-bed catalog sync

The check skill now catalogs all three CachyOS-owned OpenClaw R10 beds:
the minimal headless gateway, maximal headless tool stack, and combined
desktop deployment. Their ownership and distinct proof surfaces are explicit,
so evaluators derive the complete affected-bed roster instead of discovering
only the full bed from the wall-clock guidance.

Documentation only; the beds and their implementations remain owned by the
`box/cachyos` repository.

The git-workflow skill's attribution examples now match protected Codex policy:
the full provider model and confidence are grouped inside parentheses and
separated by a semicolon. This supersedes the shortened attribution carried by
the branch's earlier review commits; the squash commit is the sole commit that
lands on protected `main`.

Assisted-by: Codex (OpenAI GPT-5.6 Sol; documentation reviewed)

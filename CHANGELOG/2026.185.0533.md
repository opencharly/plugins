# 2026.185.0533 — go skill: declared-kind connect failure is a warn-skip, not a hard error

## Summary

The loader now WARN-SKIPS (was: hard-errored) a `kind: <plugin-word>` entity whose
declared external plugin failed to build/connect, so read-only commands (`box list`,
`validate`) work in a degraded environment (a minimal container with no Go toolchain).
Corrected the `plugin_prescan.go` description in the go skill to match.

## Correction

- **`internals/skills/go/SKILL.md`** — "A declared kind whose provider never connects is
  a LOUD error in `normalizeNodeInto`" → it is WARN-SKIPPED (a loud stderr warning + the
  node dropped, never a silent drop nor a hard load error); a command that actually USES
  the kind still fails loudly at that point.

Rides with the `charly/` code change (`node_normalize.go`) in the superproject commit.

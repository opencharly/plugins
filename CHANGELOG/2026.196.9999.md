# 2026.196.9999 — P11c doc-sweep: the plugins skills carrying the stale PodDeployTarget / in-core-OCITarget claim reflect the overlay render DISSOLUTION out of core

The plugins leg of the P11c cutover (B2 leg 1). Documentation-only (zero behavior
change); it reflects the code moves that land in the sdk leg (leg 0) +
superproject leg (leg 2) — R1 documentation-divergence-is-an-incident + R5
same-cutover claim-keyed sweep. P11c DISSOLVES the pod overlay render body OUT of
`charly/` core INTO the compiled-in `candy/plugin-deploy-pod` candy (the candy
imports `sdk/deploykit` + `sdk/buildkit` + `sdk/kit` DIRECTLY, constructs a
`deploykit.OCITarget`, renders via the shared `deploykit.NewRenderGeneratorFromProject`;
the host shrinks to a prep + resolve + envelope + cache M-seam).

## The claim-keyed sweep (R5)

The stale `PodDeployTarget`-RETAINED / `PodDeployTarget`-constructs-`OCITarget` /
in-proc-`OCITarget`-BUILD-ENGINE claim is swept across EVERY plugins skill that
repeated it (R5 claim-keyed, not identifier-only), to the post-dissolution state:
the candy `plugin-deploy-pod` renders the overlay Containerfile in its own code
via `deploykit.OCITarget` + `deploykit.NewRenderGeneratorFromProject`; NO in-proc
`DeployTarget` (Name + Emit) implementers remain in core. The 10 skills swept:

- **`internals/skills/install-plan/SKILL.md`** — source-map rows + Overview + compiler
  + `DeployTarget`-interface sections corrected: `build_overlay.go` is the
  prep+resolve+envelope+cache M-seam (`runOverlayBuild` DELETED);
  `deploy_target_pod.go` DELETED (the render moved to `candy/plugin-deploy-pod/overlay.go`);
  `build_target_oci.go` keeps only `formatDefCacheMountDefs` (the `OCITarget` walker
  is now `sdk/deploykit/oci_target.go`); the `### OCITarget (charly/build_target_oci.go)`
  section + the "Two in-proc targets ... `OCITarget` and `PodDeployTarget`" claim
  rewritten to "NO in-proc `DeployTarget` implementers remain."
- **`internals/skills/vm-deploy-target/SKILL.md`** — the `PodDeployTarget` RETAINED
  claim removed; the deploy-target table no longer asserts its retention.
- **`core/skills/deploy/SKILL.md`** — the `pod: {image:}` → `PodDeployTarget` row +
  the "retained PodDeployTarget engine" install-plan cross-ref corrected.
- **`core/skills/service/SKILL.md`** — "the Go struct is `PodDeployTarget`, the file
  is `charly/deploy_target_pod.go`" (both DELETED) corrected to the candy-owned render.
- **`internals/skills/go/SKILL.md`** — the deploy-path call-graph `OCITarget`/
  `PodDeployTarget` arm + "`OCITarget` is constructed only by `PodDeployTarget`"
  corrected (the candy's `buildOverlay` constructs `deploykit.OCITarget`).
- **`internals/skills/plugin/SKILL.md`** — the pod-overlay `OCITarget` external-step
  arm corrected to `deploykit.OCITarget` in the candy + the `ociEmitStep`
  `HostBuild("step-emit","oci-emit-step")` seam.
- **`local/skills/local-deploy/SKILL.md`** — the `PodDeployTarget` / `OCITarget`
  overlay-synthesis claim corrected to the candy render.
- **`build/skills/build/SKILL.md`** + **`build/skills/generate/SKILL.md`** — the
  `PodDeployTarget` overlay-Containerfile synthesis claims corrected to the candy
  `plugin-deploy-pod` constructing `deploykit.OCITarget` +
  `deploykit.NewRenderGeneratorFromProject`.
- **`internals/skills/generate-source/SKILL.md`** — "`OCITarget` is constructed only
  by `PodDeployTarget`" corrected to the candy's `buildOverlay`.

R5 grep self-test: `git -C plugins grep -n 'PodDeployTarget\|runOverlayBuild' -- '*.md'
:!CHANGELOG/'` returns no matches — the remaining refs are CHANGELOG history only.

## Change class + tier

Documentation-only — the 10 plugins skills above + this CHANGELOG, zero behavior
change, no Go / YAML-schema / hook / workflow code. The gate is the non-runtime
standards (adversarial consistency review, R5 grep self-test, cross-reference
validation, markdown integrity, the `pre-commit-gate.sh` / `pre-push-gate.sh`
gates). Tier: `documentation reviewed`. The superproject leg (leg 2) lands the
gitlink bump to this merged commit.

*Assisted-by: Claude (documentation reviewed)*

# 2026.196.9999 — P14-remainder doc-sweep — box-authoring (P14b) + status-collector (P14a) moves reflected in the skills

The plugins leg of the consolidated P14-remainder cutover (B2 leg 1). Both doc-sweeps are
documentation-only (zero behavior change); they reflect the code moves that land in the
superproject leg (leg 2) — R1 documentation-divergence-is-an-incident + R5 same-cutover sweep.

## P14b (scaffold half) — box-authoring move

The `charly box` AUTHORING command family moved out of `charly/` core into the NEW compiled-in
`command:authoring` plugin candy (`candy/plugin-authoring`). `charly/scaffold_cmds.go` +
`charly/scaffold_project.go` were DELETED (the verbs + the `addCandyToBox`/`removeCandyFromBox`
yaml.Node helpers + `resolveProjectFile` moved to the plugin; `box fetch`/`refresh` reenter core over
the new hidden `__box-fetch`/`__box-refresh` reentry).

- `internals/skills/go/SKILL.md` — the source map: the `scaffold_project.go` + `scaffold_cmds.go` rows
  are replaced by ONE `box_fetch_reentry.go` row documenting the hidden `__box-fetch`/`__box-refresh`
  reentry + the move to `candy/plugin-authoring` (the 5 pure verbs run on sdk/kit, zero core reentry;
  the create-side `box new` ENGINE is `kit.ScaffoldProject`/`kit.AddBox`/`kit.ScaffoldCandy`). The
  "yaml.v3 Node API" insight + the `resolveProjectFile` path-traversal-guard paragraph + the
  `/charly-build:charly-mcp-cmd` cross-ref updated to point at `candy/plugin-authoring/authoring_edit.go`
  + `sdk/kit/scaffold.go` (not the deleted `charly/scaffold_*.go`).
- `build/skills/new/SKILL.md` — the "comment-preserving" paragraph + the `/charly-internals:go`
  cross-ref updated: the create-side implementation lives in `sdk/kit/scaffold.go` + `sdk/kit/yaml.go`;
  the candy-list EDIT helpers in `candy/plugin-authoring/authoring_edit.go` (P14b).

## P14a (status half) — status-collector move

The cleanly-movable status COLLECTORS (pod live + local + the probes) moved out of `charly/` core
into `candy/plugin-substrate` (served on the substrate kind provider's `OpStatusCollect`); the
vm/k8s/android collectors + the pod deploy-enrichment stay core until K5 (deploy-cone-coupled).

- `core/skills/charly-status/SKILL.md` — the status skill doc swept to reflect the plugin-served
  collector split: the pod/local live collectors + the probes now live in `candy/plugin-substrate`
  (the `OpStatus` capability), the host keeps the kind-blind fan-out + deploy enrichment; the
  `status-substrate` seam + the K5 deferral markers documented.
- `internals/skills/go/SKILL.md` — the source map `status` row split: `status.go` →
  `candy/plugin-status/command.go` (the Kong grammar + dispatch, relocated from core, reached via the
  `status-substrate` HostBuild seam) + `charly/status_collector.go` (the collection ENGINE that stays
  core: `Collector.collectFlat` / `Collector.Single` / `enrichOne` — the pod/local live collectors
  moved to `candy/plugin-substrate`, the vm/k8s/android collectors + pod deploy-enrichment stay until K5).

This is the plugins leg of the consolidated P14-remainder cutover (superproject leg: the code moves +
the gitlink bump to this merged commit + the merged sdk). `documentation reviewed` tier (zero behavior
change — skill docs only).
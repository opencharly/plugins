# 2026.196.9999 — P11c doc-sweep: install-plan + vm-deploy-target skills reflect the overlay render DISSOLUTION out of core

The plugins leg of the P11c cutover (B2 leg 1). Documentation-only (zero behavior
change); it reflects the code moves that land in the sdk leg (leg 0) +
superproject leg (leg 2) — R1 documentation-divergence-is-an-incident + R5
same-cutover claim-keyed sweep. P11c DISSOLVES the pod overlay render body OUT of
`charly/` core INTO the compiled-in `candy/plugin-deploy-pod` candy (the candy
imports `sdk/deploykit` + `sdk/buildkit` + `sdk/kit` DIRECTLY, constructs a
`deploykit.OCITarget`, renders via the shared `deploykit.NewRenderGeneratorFromProject`;
the host shrinks to a prep + resolve + envelope + cache M-seam).

## install-plan/SKILL.md

The source-map rows for the pod overlay + target surface are corrected to the
post-dissolution state:

- **`charly/build_overlay.go`** — description fixed from the stale "host-side pod
  venue lifecycle hook" framing to the ACTUAL post-P11c role: the prep + resolve +
  envelope + cache M-seam (`hostBuildOverlay` shrinks to prep + resolve + envelope +
  cache; `runOverlayBuild` is DELETED — the render body moved to the candy).
- **`charly/deploy_target_pod.go`** — row DELETED (the file is DELETED in the super
  leg; `collectOverlayCandies` + `readImageRegistry` moved to `build_overlay.go`,
  the `PodDeployTarget` + the translate tests moved to the candy).
- **`charly/build_target_oci.go`** — row corrected: the `OCITarget` struct + all its
  methods are DELETED (the walker is now `sdk/deploykit/oci_target.go`); only
  `formatDefCacheMountDefs` remains (used by `deploy_host_helpers.go`).
- **`plugin_dispatch_reverse.go`** row aligned.

## vm-deploy-target/SKILL.md

The stale **`PodDeployTarget` RETAINED** claim is removed — `PodDeployTarget` is
DELETED in the super leg (it was a core struct the pod venue no longer needs once
the overlay render dissolves into the candy). The vm skill's deploy-target table
no longer asserts its retention.

## Change class + tier

Documentation-only — `internals/skills/install-plan/SKILL.md` +
`internals/skills/vm-deploy-target/SKILL.md` only, zero behavior change, no Go /
YAML-schema / hook / workflow code. The gate is the non-runtime standards
(adversarial consistency review, R5 grep self-test, cross-reference validation,
markdown integrity, the `pre-commit-gate.sh` / `pre-push-gate.sh` gates). Tier:
`documentation reviewed`. The superproject leg (leg 2) lands the gitlink bump to
this merged commit.

*Assisted-by: Claude (documentation reviewed)*
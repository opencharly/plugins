# 2026.202.1920 — generation-coverage list: clean.cue landed, buildctx claim fixed

## What this leg is

R5 sweep for the sdk leg's round-2 SDD fix (sdk#85 pr-validator finding):
`sdk/spec/clean_wire.go` is converted to `sdk/schema/clean.cue` and deleted,
so `/charly-internals:go`'s Generation-coverage "still hand-written
`*_wire.go`" list can no longer name `clean` — it is now a LANDED CUE
conversion, cited alongside `buildctx.cue`.

While fixing that line, also corrected a pre-existing inaccuracy on the SAME
line (R1 claim-keyed sweep — a divergence found while editing the changed
surface is fixed here too, not deferred): `buildctx` was listed in the
parenthetical of STILL-hand-written stems even though the very next clause
already calls `buildctx.cue` "the landed reference" — an internal
contradiction on one line. `buildctx` is removed from the still-hand-written
list; the sentence now names both `buildctx.cue` and `clean.cue` as LANDED.

## Verification

Documentation-only change class: no runtime behavior in this repo. The
claim itself (clean.cue exists, clean_wire.go is deleted, byte-for-byte
field parity) is verified against the sdk leg's actual diff (sdk#85, commit
`0ecfe18`).

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; documentation reviewed)

# 2026.193.0458 — all 6 roster agents get the `Skill` tool; pr-validator loads its skills BY NAME (Option A)

Every charly roster agent (`internals/agents/*.md`) now carries `Skill` in its `tools:` frontmatter —
check-bed-runner, deploy-verifier, layer-validator, pr-validator, root-cause-analyzer, testing-validator —
so a spawned agent invokes the charly skills BY NAME via the `Skill` tool instead of `Read`ing each
`SKILL.md` by path. Live-verified on this host: agent specs load at SESSION START (a spec edit needs a
session restart to reach freshly-spawned agents), and a post-restart pr-validator probe confirmed the
`Skill` tool present and `charly-internals:git-workflow` loading by name.

- **All 6 agent specs** — `tools:` gains `Skill` (additive; each agent's existing tool lane is unchanged).
- **All 6 agent specs** — `tools:` ALSO gains `SendMessage` (operator-directed extension): the probe
  demonstrated a restricted agent literally cannot report back (`SendMessage exists but is not enabled in
  this context` — its verdict lived only in its transcript). The 5 Bash-holding agents additionally gain
  `Write` (the dedicated tool for their durable report/verdict files); `layer-validator` keeps its
  read-only lane (no Bash/Write). pr-validator's "When to Invoke" now instructs reporting over EVERY
  channel — SendMessage to the delegating lead when enabled, with the verdict file + PR comment staying
  the mandatory durable channels. LIVE-VERIFIED without a restart: specs load at session start, so a fresh
  headless `claude -p` child session (its own fresh session start) spawned testing-validator from the
  edited spec — spawn succeeds with the new `tools:` line, `Write` works, and `SendMessage` is ENABLED
  (its only failure was addressing — no teammate named `team-lead` existed in the probe session — not the
  pre-change `No such tool available` gating).
- **`pr-validator`** — a new "R0 — load your PR-validation skills FIRST" section: before Phase 1, load
  `charly-internals:git-workflow` (mandatory) plus every skill the change's area triggers per the CLAUDE.md
  Skill Dispatcher, ALL in one step, and validate against the skill TEXT, never from memory. If the `Skill`
  tool is ever unavailable, fall back to `Read`ing each skill's `SKILL.md` by path — never validate
  skills-blind.

Rationale: a restricted (Read/Bash/Grep) agent had NO `Skill` tool, forcing spawn prompts to spell out
SKILL.md paths; a Tools:* agent already invoked skills by name (probed 2026-07-12). Option A closes the gap
for the whole roster — skill access is now uniform BY NAME, and the Read-by-path fallback remains documented
for genuinely Skill-less contexts.

# 2026.194.2100 — docs batch: statekit delivered-as-kit sweep + gate-note + changelog intro fix

Three registered corrections batched into one docs cutover:

- **`/charly-internals:plugin` statekit sweep (R1 — the K4-B ruling's divergence):** the
  three claims that state "is/will be `sdk/statekit`" corrected — the flock'd,
  any-process-safe state family was DELIVERED in `sdk/kit` (`filelock.go` +
  `install_ledger.go` + `deployconfig.go`, P4/C9); the K4-B decomposition ruled a
  separate `statekit` package pure churn. The K2 ledger row now records the delivered
  state + the residual routing (loader-coupled halves → K5 seam-death; lifecycle body →
  K4-A).
- **`/charly-internals:git-workflow` B7 gate-note:** the `pre-commit-gate` mechanically
  blocks `git -C "$VAR" commit` forms and asks for the literal absolute path — a parse
  requirement with ONE sanctioned re-issue, distinguished from the forbidden
  reshape-and-retry.
- **CHANGELOG/2026.194.1841.md intro corrected:** "four operator-escalated findings" →
  "six wave-3 orchestration findings (three operator-escalated + three RCA-derived)" —
  matching its own bullet list (ledger discipline, TaskStop, rooting = operator-escalated;
  the preventive trio = RCA-derived).

*Assisted-by: Claude (documentation reviewed)*

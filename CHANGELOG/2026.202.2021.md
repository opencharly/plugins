# 2026.202.2021 — clean --deep documentation + generation-coverage sweep (plugins leg)

Finalized by the fresh `pr-validator` at merge.

## What this leg is

Documentation for the `charly clean --deep` gap-closing cutover (the
superproject leg adds the `--deep` flag + engine; the sdk leg adds the wire
fields it carries), consolidated into ONE placeholder covering both the
docs update and the follow-on generation-coverage sweep the sdk leg's
round-2 SDD fix required:

- `core/skills/clean/SKILL.md` — documents the new `--deep` flag, its
  store-wide untagged/dangling-image purge semantics (closing the gap that
  the default charly-labeled sweep can never see an unlabeled multi-stage
  build intermediate), the `--dry-run` reclaimable-bytes report, and the
  shared `pruneDanglingImages`/`selectDanglingImages` engine both the default
  sweep and `--deep` now route through (R3).
- `internals/skills/strict-policy/SKILL.md` — adds the R4 replacement-table
  row `podman image prune` → `charly clean --deep`, the motivating gap this
  cutover closes (the CLI-only-interface mandate's gap-closing path).
- `internals/skills/go/SKILL.md` — R5 sweep for the sdk leg's round-2 SDD
  fix (sdk#85 pr-validator finding): `sdk/spec/clean_wire.go` is converted
  to `sdk/schema/clean.cue` and deleted, so the Generation-coverage "still
  hand-written `*_wire.go`" list can no longer name `clean` — it is now a
  LANDED CUE conversion, cited alongside `buildctx.cue`. While fixing that
  line, also corrected a pre-existing inaccuracy on the SAME line (R1
  claim-keyed sweep — a divergence found while editing the changed surface
  is fixed here too, not deferred): `buildctx` was listed in the
  parenthetical of STILL-hand-written stems even though the very next
  clause already calls `buildctx.cue` "the landed reference" — an internal
  contradiction on one line. `buildctx` is removed from the
  still-hand-written list; the sentence now names both `buildctx.cue` and
  `clean.cue` as LANDED.

**R1 correction (same cutover):** the orchestrator's destructive R10 run
showed the reclaimable-bytes figure heavily overstates actual freed disk
(removed images routinely share layers with the many that remain). Both
skill docs now label the figure "up to X (shared layers may reduce actual
reclaim)" and note that pairing `--deep` with `--invalidate` gets closer to
the reported total.

## Verification

Documentation-only change class: no runtime behavior in this repo. Reviewed
for consistency against the paired superproject + sdk legs' actual
implementation (flag names, reply field names, and the live
`charly clean --deep --dry-run` proof pasted in the superproject leg's
CHANGELOG entry). The generation-coverage claim (clean.cue exists,
clean_wire.go is deleted, byte-for-byte field parity) is verified against
the sdk leg's actual diff (sdk#85, merged `main` `52e7a09`, tag
`v0.2026202.1944`). Landing order: sdk#85 and the paired charly leg (#174,
merged `main` `ed487ff`, tag `v2026.202.2007`) are both already merged —
this leg documents landed facts.

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; documentation reviewed)

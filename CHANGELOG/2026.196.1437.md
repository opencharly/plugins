# 2026.196.1437 — Share native plugins across Claude Code and Codex

- Add one idempotent project-file setup command with developer, user, and container-family profiles; user configuration is never touched.
- Add native Codex manifests for every plugin while keeping one shared skills tree and the existing MCP companions.
- Make full developer mode the default and validate all marketplace plugins and portable skills.
- Give four VM skills globally unique names and repair stale skill references and frontmatter.
- Make each plugin manifest the single release-version authority and correct the Jupyter version mismatch.
- Keep the existing MCP manifests and server wiring unchanged.
- Correct VM/check guidance for strict missing-target destruction and the explicit idempotent `--if-exists` reconciliation mode.
- Require targetless-group member rebuild and final teardown to appear as failing-capable R10 summary steps.

Assisted-by: Codex OpenAI GPT-5.6 Sol (fully tested and validated)

# M5 doctrine finale: kernel/plugin doctrine into the skills

Cutover 5 (docs-only) — the core/plugin doctrine written into the skill layer. Added the
"Remaining program — kernel-minimization roadmap (current state)" section to the plugin skill
(the doctrine is substantially realized — SDK boundary, unified authoring, all substrates +
commands + subsystems are plugins; the pod lifecycle is externalized; the vm lifecycle is the last
in-flight substrate-lifecycle externalization; registry+merge / status / alias / scaffold are
classified candidates). Added the kernel/plugin-doctrine row to the skills skill's Authoritative-copy
registry (sole owner /charly-internals:plugin). Pairs with the CLAUDE.md Key Rule + the VISION tenet
(main repo).

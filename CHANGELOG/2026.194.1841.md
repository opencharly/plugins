# 2026.194.1841 — orchestration findings: ledger discipline, TaskStop semantics, rooting scope-of-validity

Codifies six wave-3 orchestration findings (four operator-escalated + two RCA-derived) into the skills:

- **`/charly-internals:agents` "Migration-ledger discipline" (new subsection):** register
  rows are conflated-until-measured (variances −79%/−45%/~−80%/+60% in four consecutive
  scoping rounds); a measured per-file scoping map precedes every cut; an enabler is named
  by its exact SURFACE (def/fields/op), never a subsystem word, and every row naming an
  enabler is re-audited when it lands; every plan-vs-measured mismatch is a BLOCKING
  incident with a dedicated root-cause-analyzer RCA; a gap inside an assigned cutover
  means BUILD the enabler in the cutover (spike-first, growth-with-consumers) — never
  clean-slice-plus-deferral; exclusions only as per-file boundary-law justifications.
- **`/charly-internals:agents` "Agent lifecycle hygiene":** a `shutdown_request` message
  is NOT a stop — the process and pane persist until `TaskStop(<name>)`; fire it the
  moment work is accepted (the observed leak: 8 finished wave-teammates idling for hours).
- **Rooting-claim scope-of-validity (claim-keyed sweep, R1 — ALL FOUR surfaces carrying
  the claim: `/charly-internals:agents` invariant #1, `/charly-internals:git-workflow` B5
  ×2 (the rooting-consequences bullet AND the autonomous-landing contract), and the
  `pr-validator` agent definition W1):** scope-of-validity correction to the
  submodule-rooting denial claim — USER/MANAGED-level settings (e.g. the operator's `autoMode.allow` rule) resolve
  independently of project root; a submodule-rooted validator under that rule posted
  `success` and merged with zero denials. Superproject rooting remains the rule; diagnose
  a denial by checking both settings layers, never root alone.

- **RCA-LOC estimator fixes (the dedicated variance RCA's preventive trio):** a register
  row quotes NO collectible LOC until its per-file boundary-law decomposition exists
  (undecomposed = UNMEASURED, an authoring-time rule); and same-signed variance recurring
  across ≥2 rounds is a systemic ESTIMATOR defect triggering a dedicated RCA on the
  estimating method itself — the per-merge ">20% explain + reassign" rule handles one
  variance and cannot see a series.
- **Bidirectional call-graph sizing (the RCA's generalized root cause):** a body's
  file-LOC is a floor, never the move — outbound enabler-fit over-counts collectibility,
  inbound callee drag under-counts move size; scoping maps read the call-graph in both
  directions.

*Assisted-by: Claude (documentation reviewed)*
